<?php
    //This is generated by Swapp Software Application on 02/05/2019 12:10:18 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $bankcode = $_GET['bankcode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       if ($bankcode=='')
       $query = "select bankbranchcode,
       bankbranchnameuni,
       bankbranchnameeng,
       shortname,ifsc
       from nst_nasaka_agriculture.bankbranch r,nst_nasaka_agriculture.bank b
       where r.bankcode=b.bankcode
       order by bankbranchnameuni";
       else
       $query = "select bankbranchcode,
       bankbranchnameuni,
       bankbranchnameeng,
       shortname
       from nst_nasaka_agriculture.bankbranch r,nst_nasaka_agriculture.bank b
       where r.bankcode=b.bankcode and b.bankcode=".$bankcode." order by bankbranchnameuni";
   }
   else
   {
       if ($bankcode!='')
       $query = "select bankbranchcode,
       bankbranchnameuni,
       bankbranchnameeng,
       shortname,ifsc
       from nst_nasaka_agriculture.bankbranch r,nst_nasaka_agriculture.bank b
       where r.bankcode=b.bankcode and b.bankcode=".$bankcode." and ((upper(bankbranchnameeng) like '%".$searchTerm."%')
       or bankbranchnameuni like '%".$searchTerm."%'
       or to_char(bankbranchcode) like '".$searchTerm."'
       or (upper(banknameeng) like '%".strtoupper($searchTerm)."%')
       or (upper(bankbranchnameeng) like '%".strtoupper($searchTerm)."%')
       or shortname like '%".strtoupper($searchTerm)."%') order by bankbranchnameuni";
       else
       $query = "select bankbranchcode,
       bankbranchnameuni,
       bankbranchnameeng,
       shortname,ifsc
       from nst_nasaka_agriculture.bankbranch r,nst_nasaka_agriculture.bank b
       where r.bankcode=b.bankcode and ((upper(bankbranchnameeng) like '%".strtoupper($searchTerm)."%')
       or bankbranchnameuni like '%".$searchTerm."%'
       or to_char(bankbranchcode) like '".$searchTerm."'
       or (upper(banknameeng) like '%".strtoupper($searchTerm)."%')
       or (upper(bankbranchnameeng) like '%".strtoupper($searchTerm)."%')
       or upper(shortname) like '%".strtoupper($searchTerm)."%') order by bankbranchnameuni"; 
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['BANKBRANCHCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['SHORTNAME'].', Branch '.$row['BANKBRANCHNAMEENG'].' '.$row['IFSC'];
       }
       else
       {
           $name = $row['SHORTNAME'].', शाखा '.$row['BANKBRANCHNAMEUNI'].' '.$row['IFSC'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

