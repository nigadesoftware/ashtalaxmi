<?php
    //This is generated by Swapp Software Application on 09/01/2021 01:58:43 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select EMPLOYEECODE,
       EMPLOYEENAMEUNI,
       EMPLOYEENAMEENG
       from EMPLOYEEmaster";
   }
   else
   {
       $query = "select EMPLOYEECODE,
       EMPLOYEENAMEUNI,
       EMPLOYEENAMEENG
       from EMPLOYEEmaster where (upper(EMPLOYEENAMEENG) like upper('%".$searchTerm."%')
       or EMPLOYEENAMEUNI like '%".$searchTerm."%'
       or to_char(EMPLOYEECODE) like '".$searchTerm."')";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['EMPLOYEECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['EMPLOYEENAMEENG'];
       }
       else
       {
           $name = $row['EMPLOYEENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

