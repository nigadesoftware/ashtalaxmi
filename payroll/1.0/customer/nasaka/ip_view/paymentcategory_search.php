<?php
    //This is generated by Swapp Software Application on 03/09/2022 03:59:12 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select paymentcategoryCODE,
       paymentcategoryNAMEUNI,
       paymentcategoryNAMEENG
       from paymentcategory ";
   }
   else
   {
       $query = "select paymentcategoryCODE,
       paymentcategoryNAMEUNI,
       paymentcategoryNAMEENG
       from paymentcategory where (upper(paymentcategoryNAMEENG) like upper('%".$searchTerm."%')
       or paymentcategoryNAMEUNI like '%".$searchTerm."%'
       or to_char(paymentcategoryCODE) like '".$searchTerm."') ";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['PAYMENTCATEGORYCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['PAYMENTCATEGORYNAMEENG'];
       }
       else
       {
           $name = $row['PAYMENTCATEGORYNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

