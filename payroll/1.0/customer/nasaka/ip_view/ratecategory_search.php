<?php
    //This is generated by Swapp Software Application on 14/02/2023 10:54:02 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select RATECATEGORYCODE,
       RATECATEGORYNAMEUNI,
       RATECATEGORYNAMEENG
       from RATECATEGORY where rownum <= 10";
   }
   else
   {
       $query = "select RATECATEGORYCODE,
       RATECATEGORYNAMEUNI,
       RATECATEGORYNAMEENG
       from RATECATEGORY where (upper(RATECATEGORYNAMEENG) like upper('%".$searchTerm."%')
       or RATECATEGORYNAMEUNI like '%".$searchTerm."%'
       or to_char(RATECATEGORYCODE) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['RATECATEGORYCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['RATECATEGORYNAMEENG'];
       }
       else
       {
           $name = $row['RATECATEGORYNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

