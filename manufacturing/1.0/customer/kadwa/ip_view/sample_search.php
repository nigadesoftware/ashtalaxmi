<?php
    //This is generated by Swapp Software Application on 27/12/2021 08:19:48 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select SAMPLECODE,
       SAMPLENAMEUNI,
       SAMPLENAMEENG
       from SAMPLE where rownum <= 10";
   }
   else
   {
       $query = "select SAMPLECODE,
       SAMPLENAMEUNI,
       SAMPLENAMEENG
       from SAMPLE where (upper(SAMPLENAMEENG) like upper('%".$searchTerm."%')
       or SAMPLENAMEUNI like '%".$searchTerm."%'
       or to_char(SAMPLECODE) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['SAMPLECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['SAMPLENAMEENG'];
       }
       else
       {
           $name = $row['SAMPLENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

