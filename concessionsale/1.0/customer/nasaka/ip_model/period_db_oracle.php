<?php
//This is generated by Nigade Software Technologies (opc) Private Limited 19/05/2021 07:30:06 pm for PHP
require_once("../swappbase/formbase.php");
class period extends swappform
{
    Public $periodcode;
    Public $perionameeng;
    Public $perionameuni;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->periodcode='';
         $this->periodcodetextcombo='';
         $this->perionameeng='';
         $this->perionameengtextcombo='';
         $this->perionameuni='';
         $this->perionameunitextcombo='';
	   }
    public function entryvalidation()
    {
    		$this->start_validation();
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->periodcode == '')
        {
            $query = "select nvl(max(periodcode),0)+1 as periodcode from period";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->periodcode = $row["PERIODCODE"];
        }
        $query = "
            insert into period(
            periodcode
            ,perionameeng
            ,perionameuni
	        )
	        values (
            ".$this->invl($this->periodcode,true)."
            ,".$this->invl($this->perionameeng,false)."
            ,".$this->invl($this->perionameuni,false)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->periodcode!='' and $this->periodcode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.periodcode = ".$this->periodcode;
			       }
			       else
			       {
				         $cond=$cond." and t.periodcode = ".$this->periodcode;
			       }
		    }
		    if ($this->perionameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.perionameeng like '%".$this->perionameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and t.perionameeng like '%".$this->perionameeng."%'";
			       }
		    }
		    if ($this->perionameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.perionameuni like '%".$this->perionameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and t.perionameuni like '%".$this->perionameuni."%'";
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from period t where ".$cond." order by periodcode,perionameeng,perionameuni";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from period t order by periodcode,perionameeng,perionameuni";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update period set 
        where 
        periodcode = ".$this->invl($this->periodcode,true)."
         and perionameeng ='".$this->invl($this->perionameeng,false)."'
         and perionameuni ='".$this->invl($this->perionameuni,false)."'
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from period
        where 
        periodcode ='".$this->invl($this->periodcode,false)."'
         and perionameeng ='".$this->invl($this->perionameeng,false)."'
         and perionameuni ='".$this->invl($this->perionameuni,false)."'
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        periodcode
        ,perionameeng
        ,perionameuni
        from period where 
        periodcode = ".$this->invl($this->periodcode,true)."
         and perionameeng ='".$this->invl($this->perionameeng,false)."'
         and perionameuni ='".$this->invl($this->perionameuni,false)."'
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->periodcode = $row['PERIODCODE'];
                $this->perionameeng = $row['PERIONAMEENG'];
                $this->perionameuni = $row['PERIONAMEUNI'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

