<?php
    //This is generated by Swapp Software Application on 25/05/2019 06:15:01 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (isset($_GET['circlecode']))
    {
        $circlecode=$_GET['circlecode'];
    }
    else
    {
        $circlecode=0;
    }
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       if ($circlecode==0)
       {
            $query = "select centrecode,
            centrenameuni,
            centrenameeng
            from nst_nasaka_agriculture.vw_centre
            order by centrenameuni";
       }
       elseif ($circlecode>0)
       {
            $query = "select centrecode,
            centrenameuni,
            centrenameeng
            from nst_nasaka_agriculture.vw_centre
            where circlecode=".$circlecode.
            " order by centrenameuni";
       }
   }
   else
   {
        if ($circlecode==0)
        {
            $query = "select centrecode,
            centrenameuni,
            centrenameeng
            from nst_nasaka_agriculture.vw_centre where (upper(centrenameeng) like upper('%".$searchTerm."%')
            or centrenameuni like '%".$searchTerm."%'
            or to_char(centrecode) like '".$searchTerm."') order by centrenameuni";
        }
        elseif ($circlecode>0)
        {
            $query = "select centrecode,
            centrenameuni,
            centrenameeng
            from nst_nasaka_agriculture.vw_centre where circlecode={$circlecode}
            and (upper(centrenameeng) like upper('%".$searchTerm."%')
            or centrenameuni like '%".$searchTerm."%'
            or to_char(centrecode) like '".$searchTerm."') order by centrenameuni";
        }
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['CENTRECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['CENTRENAMEENG'];
       }
       else
       {
           $name = $row['CENTRENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

