<?php
    //This is generated by Swapp Software Application on 24/05/2021 06:48:17 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select CONCESSIONCATEGORYCODE,
       CONCESSIONCATEGORYNAMEUNI,
       CONCESSIONCATEGORYNAMEENG
       from CONCESSIONCATEGORY where rownum <= 10";
   }
   else
   {
       $query = "select CONCESSIONCATEGORYCODE,
       CONCESSIONCATEGORYNAMEUNI,
       CONCESSIONCATEGORYNAMEENG
       from CONCESSIONCATEGORY where (upper(CONCESSIONCATEGORYNAMEENG) like upper('%".$searchTerm."%')
       or CONCESSIONCATEGORYNAMEUNI like '%".$searchTerm."%'
       or to_char(CONCESSIONCATEGORYCODE) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['CONCESSIONCATEGORYCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['CONCESSIONCATEGORYNAMEENG'];
       }
       else
       {
           $name = $row['CONCESSIONCATEGORYNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

