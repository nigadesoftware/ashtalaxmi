<?php
    //This is generated by Swapp Software Application on 25/05/2019 06:15:01 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (isset($_GET['circlecode']))
    {
        $circlecode=$_GET['circlecode'];
        $centrecode=0;
    }
    elseif (isset($_GET['centrecode']))
    {
        $centrecode=$_GET['centrecode'];
        $circlecode=0;
    }
    else
    {
        $circlecode=0;
        $centrecode=0;
    }
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       if ($circlecode==0 and $centrecode==0)
       {
            $query = "select villagecode,
            villagenameuni,
            villagenameeng
            from nst_nasaka_agriculture.village
            order by villagenameuni";
       }
       elseif ($circlecode>0)
       {
            $query = "select villagecode,
            villagenameuni,
            villagenameeng
            from nst_nasaka_agriculture.village
            where circlecode=".$circlecode.
            " order by villagenameuni";
       }
       elseif ($centrecode>0)
       {
            $query = "select villagecode,
            villagenameuni,
            villagenameeng
            from nst_nasaka_agriculture.village
            where centrecode=".$centrecode." order by villagenameuni";
       }
   }
   else
   {
        if ($circlecode==0 and $centrecode==0)
        {
            $query = "select villagecode,
            villagenameuni,
            villagenameeng
            from nst_nasaka_agriculture.village where (upper(villagenameeng) like upper('%".$searchTerm."%')
            or villagenameuni like '%".$searchTerm."%'
            or to_char(villagecode) like '".$searchTerm."') order by villagenameuni";
        }
        elseif ($circlecode>0)
        {
            $query = "select villagecode,
            villagenameuni,
            villagenameeng
            from nst_nasaka_agriculture.village where circlecode={$circlecode}
            and (upper(villagenameeng) like upper('%".$searchTerm."%')
            or villagenameuni like '%".$searchTerm."%'
            or to_char(villagecode) like '".$searchTerm."') order by villagenameuni";
        }
        elseif ($centrecode>0)
        {
            $query = "select villagecode,
            villagenameuni,
            villagenameeng
            from nst_nasaka_agriculture.village where centrecode={$centrecode}
            and (upper(villagenameeng) like upper('%".$searchTerm."%')
            or villagenameuni like '%".$searchTerm."%'
            or to_char(villagecode) like '".$searchTerm."') order by villagenameuni";
        }
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['VILLAGECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['VILLAGENAMEENG'];
       }
       else
       {
           $name = $row['VILLAGENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

