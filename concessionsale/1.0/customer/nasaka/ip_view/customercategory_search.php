<?php
    //This is generated by Swapp Software Application on 29/05/2021 10:50:46 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select CUSTOMERCATEGORYCODE,
       CUSTOMERCATEGORYNAMEUNI,
       CUSTOMERCATEGORYNAMEENG
       from CUSTOMERCATEGORY where rownum <= 10";
   }
   else
   {
       $query = "select CUSTOMERCATEGORYCODE,
       CUSTOMERCATEGORYNAMEUNI,
       CUSTOMERCATEGORYNAMEENG
       from CUSTOMERCATEGORY where (upper(CUSTOMERCATEGORYNAMEENG) like upper('%".$searchTerm."%')
       or CUSTOMERCATEGORYNAMEUNI like '%".$searchTerm."%'
       or to_char(CUSTOMERCATEGORYCODE) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['CUSTOMERCATEGORYCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['CUSTOMERCATEGORYNAMEENG'];
       }
       else
       {
           $name = $row['CUSTOMERCATEGORYNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

