<?php
    //This is generated by Swapp Software Application on 01/03/2019 08:32:54 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (isset($_GET['saleorderdate']))
    {
        $saleorderdate = $_GET['saleorderdate'];
        $saleorderdate = DateTime::createFromFormat('d/m/Y',$saleorderdate)->format('d-M-Y');
    }
    else
    {
        $saleorderdate = '';
    }
    if (isset($_GET['quotationdate']))
    {
        $quotationdate = $_GET['quotationdate'];
        $quotationdate = DateTime::createFromFormat('d/m/Y',$quotationdate)->format('d-M-Y');
    }
    else
    {
        $quotationdate = '';
    }
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
    /*elseif (strlen($saleorderdate)==0)
    {
        exit;
    }*/
   if ($searchTerm == '**' and strlen($saleorderdate)>0)
   {
       $query = "select TRANSACTIONNUMBER,
       tendernumberpresuf,
       tendernumber,
       tenderdate
       from saletenderheader where tenderclosedate>='".$saleorderdate."'
       order by tenderdate desc,tendernumber desc";
   }
   elseif ($searchTerm == '**' and strlen($quotationdate)>0)
   {
       $query = "select TRANSACTIONNUMBER,
       tendernumberpresuf,
       tendernumber,
       tenderdate
       from saletenderheader where tenderclosedate>='".$quotationdate."'
       order by tenderdate desc,tendernumber desc";
   }
   elseif ($searchTerm == '**' and strlen($saleorderdate)==0 and strlen($quotationdate)==0)
   {
       $query = "select TRANSACTIONNUMBER,
       tendernumberpresuf,
       tendernumber,
       tenderdate
       from saletenderheader
       order by tenderdate desc,tendernumber desc";
   }
   elseif (strlen($saleorderdate)>0)
   {
       $query = "select TRANSACTIONNUMBER,
       tendernumberpresuf,
       tendernumber,
       tenderdate
       from saletenderheader where (upper(tendernumber) like upper('%".$searchTerm."%')
       or tendernumber like '%".$searchTerm."%'
       or to_char(TRANSACTIONNUMBER) like '".$searchTerm."') and tenderclosedate>='".$saleorderdate."'
       order by tenderdate desc,tendernumber desc";
   }
   elseif (strlen($quotationdate)>0)
   {
       $query = "select TRANSACTIONNUMBER,
       tendernumberpresuf,
       tendernumber,
       tenderdate
       from saletenderheader where (upper(tendernumber) like upper('%".$searchTerm."%')
       or tendernumber like '%".$searchTerm."%'
       or to_char(TRANSACTIONNUMBER) like '".$searchTerm."') and tenderclosedate>='".$quotationdate."'
       order by tenderdate desc,tendernumber desc";
   }
   
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['TRANSACTIONNUMBER'];
       $tenderdate = DateTime::createFromFormat('d-M-Y',$row['TENDERDATE'])->format('d/m/Y');
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['TENDERNUMBERPRESUF'].' ('.$tenderdate.')';
       }
       else
       {
           $name = $row['TENDERNUMBERPRESUF'].' ('.$tenderdate.')';
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

