<?php
    //This is generated by Swapp Software Application on 01/03/2019 08:16:54 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $yearperiodcode = $_SESSION['yearperiodcode'];
    $goodscategorycode = $_GET['goodscategorycode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<1 and $searchTerm != '**')
    {
        exit;
    }
    $cond ='othmatcategorycode='.$goodscategorycode.' and yearperiodcode='.$yearperiodcode;
    /* if ($goodscategorycode==2)
        $cond='othmatcategorycode=8'; */
        //w.yearperiodcode=".$yearperiodcode." and
            $query = "select w.receiptnumber,w.loadweight,w.emptyweight,w.netweight,w.vehiclenumber 
            from nst_nasaka_agriculture.otherweight w 
            where (w.receiptnumber=".$searchTerm.") and {$cond}";

       $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['RECEIPTNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['RECEIPTNUMBER'];
       }
       else
       {
           $name = $row['RECEIPTNUMBER'];
       }
       $loadweight = number_format($row['LOADWEIGHT'],3,'.',',');
       $emptyweight = number_format($row['EMPTYWEIGHT'],3,'.',',');
       $netweight = number_format($row['NETWEIGHT'],3,'.',',');
       $vehiclenumber = $row['VEHICLENUMBER'];
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           ,'loadweight' => $loadweight
           ,'emptyweight' => $emptyweight
           ,'netweight' => $netweight
           ,'vehiclenumber' => $vehiclenumber
           );
   }
   //return json data
   echo json_encode($data);
?>

