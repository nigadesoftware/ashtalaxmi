<?php
    //This is generated by Swapp Software Application on 01/03/2019 08:16:54 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select t.godownnumber
       ,t.godownnameeng
       ,t.godownnameuni from godown t";
   }
   else
   {
       $query = "select t.godownnumber
       ,t.godownnameeng
       ,t.godownnameuni from godown t where  (upper(godownnameeng) like upper('%".$searchTerm."%')
       or godownnameuni like '%".$searchTerm."%'
       or to_char(godownnumber) like '".$searchTerm."')";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['GODOWNNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['GODOWNNAMEENG'];
       }
       else
       {
           $name = $row['GODOWNNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

