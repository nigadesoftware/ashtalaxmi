<?php
    //This is generated by Swapp Software Application on 01/03/2019 08:32:54 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $tendertransactionnumber = $_GET['tendertransactionnumber'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select TRANSACTIONNUMBER
       ,quotationnumberpresuf
       ,quotationnumber
       ,h.purchasercode
       ,purchasernameeng
       ,purchasernameuni
       from salequotationheader h
       ,goodspurchaser s 
       where s.goodscategorycode=2
       and h.purchasercode=s.purchasercode";
   }
   else
   {
       $query = "select TRANSACTIONNUMBER
       ,quotationnumberpresuf
       ,quotationnumber
       ,h.purchasercode
       ,purchasernameeng
       ,purchasernameuni
       from salequotationheader h
       ,goodspurchaser s
        where (upper(quotationnumber) like upper('%".$searchTerm."%')
       or quotationnumber like '%".$searchTerm."%'
       or to_char(TRANSACTIONNUMBER) like '".$searchTerm."') 
       and tendertransactionnumber=".$tendertransactionnumber." 
       and s.goodscategorycode=2
       and h.purchasercode=s.purchasercode";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['TRANSACTIONNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['QUOTATIONNUMBERPRESUF'];
           $purchasername = $row['PURCHASERNAMEENG'];
           $label = $row['QUOTATIONNUMBERPRESUF'].' '.$row['PURCHASERNAMEENG'];
       }
       else
       {
           $name = $row['QUOTATIONNUMBERPRESUF'];
           $purchasername = $row['PURCHASERNAMEUNI'];
           $label = $row['QUOTATIONNUMBERPRESUF'].' '.$row['PURCHASERNAMEUNI'];
       }
       $purchasercode = $row['PURCHASERCODE'];
       $data[] = array( 
           'id' => $id
           ,'label' => $label
           ,'value' => $name
           ,'purchasername' => $purchasername
           ,'purchasercode' => $purchasercode
           );
   }
   //return json data
   echo json_encode($data);
?>

