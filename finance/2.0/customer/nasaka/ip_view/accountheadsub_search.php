<?php
    //This is generated by Swapp Software Application on 16/11/2018 08:48:59 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
    if ($searchTerm == '**')
    {
       $query = "select accountcode,
       accountnameuni,
       accountnameeng
       from accounthead
       where issubledgerallowed=248805511";
    }
    else
    {
       $query = "select accountcode,
       accountnameuni,
       accountnameeng
       from accounthead where (upper(accountnameeng) like upper('%".$searchTerm."%')
       or accountnameuni like '%".$searchTerm."%'
       or to_char(accountcode) = '".$searchTerm."'
       or refcode = '".$searchTerm."') and issubledgerallowed=248805511 and rownum <= 100";
    }
    $result = oci_parse($connection, $query); $r = oci_execute($result);
    $r = oci_execute($result);
    while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
    {
       $id = $row['ACCOUNTCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['ACCOUNTNAMEENG'];
       }
       else
       {
           $name = $row['ACCOUNTNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
    }
   //return json data
   echo json_encode($data);
?>

