<?php
    //This is generated by Swapp Software Application on 11/11/2018 04:47:25 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =-----_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3)
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select applicationnumber,
       applicant,
       applicant
       from applicationheader where rownum <= 10";
   }
   else
   {
       $query = "select applicationnumber,
       applicant,
       applicant
       from applicationheader where (upper(applicant) like upper('%".$searchTerm."%')
       or applicant like '%".$searchTerm."%'
       or to_char(applicationnumber) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['APPLICATIONNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['APPLICANT'];
       }
       else
       {
           $name = $row['APPLICANT'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

