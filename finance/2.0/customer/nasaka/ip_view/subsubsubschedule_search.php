<?php
    //This is generated by Swapp Software Application on 16/11/2018 08:48:59 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_POST['get_option'];
       $query = "select subsubsubschedulecode,
       subsubsubschedulenameuni,
       subsubsubschedulenameeng
       from accountsubsubsubschedule
       where subsubschedulecode=".$searchTerm." 
       order by subsubsubschedulenameuni";
   $result = oci_parse($connection, $query);
   $r = oci_execute($result);
   $i=1;
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       if ($i++==1)
       {
            if ($_SESSION['lng']=="English")
            {
                echo '<option value="-1">[Select]</option>';
            }
            else
            {
                echo '<option value="-1">[निवडा]</option>';
            }
       }
       $id = $row['SUBSUBSUBSCHEDULECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['SUBSUBSUBSCHEDULENAMEENG'];
       }
       else
       {
           $name = $row['SUBSUBSUBSCHEDULENAMEUNI'];
       }
       echo '<option value="'.$id.'">'.$name.'</option>';
   }
   exit;
?>