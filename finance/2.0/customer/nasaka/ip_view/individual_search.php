<?php
    //This is generated by Swapp Software Application on 16/11/2018 08:48:59 PM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if ($searchTerm=='')
    {
        exit;
    }
       $query = "select subledgernameuni,
       subledgernameeng,
       referencecode
       from accountsubledger 
       where upper(subledgernameeng) like '%".strtoupper($searchTerm)."%' 
       or subledgernameuni like '%".strtoupper($searchTerm)."%' 
       or upper(referencecode) = '".strtoupper($searchTerm)."' 
       and rownum <= 20
       group by subledgernameuni,
       subledgernameeng,
       referencecode";
   $result = oci_parse($connection, $query); 
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
        if ($_SESSION['lng'] == "English")
        {
            $name = $row['SUBLEDGERNAMEENG'];
        }
        else
        {
            $name = $row['SUBLEDGERNAMEUNI'];
        }   
        $referencecode = $row['REFERENCECODE'];

       $data[] = array( 
           'id' => $referencecode
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

