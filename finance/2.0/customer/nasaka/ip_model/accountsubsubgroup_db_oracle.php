<?php
//This is generated by Swapp Software Application on 10/11/2018 06:06:11 PM for PHP
include("../swappbase/formbase.php");
class accountsubsubgroup extends swappform
{
    Public $subsubgroupcode;
    Public $subsubgroupnameeng;
    Public $subsubgroupnameuni;
    Public $groupcode;
    Public $subgroupcode;
    public function __construct(&$connection)
    {
     	 parent::__construct($connection);
         $this->subsubgroupcode='';
         $this->subsubgroupnameeng='';
         $this->subsubgroupnameuni='';
         $this->groupcode='';
         $this->subgroupcode='';
	}
    private function entryvalidation()
    {
    	$this->start_validation();
        $this->checkrequired($this->subsubgroupnameeng,'Sub sub group name(English)');
        $this->checkrequired($this->subsubgroupnameuni,'Sub sub group name(Unicode)');
        $this->checkrequired($this->groupcode,'Group code');
        $this->checkrequired($this->subgroupcode,'Sub group code');
        $this->end_validation();
        return $this->invalidid;
	}
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->subsubgroupcode == '')
        {
            $query = "select nvl(max(subsubgroupcode),0)+1 as subsubgroupcode from accountsubsubgroup";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->subsubgroupcode = $row["SUBSUBGROUPCODE"];
        }
        $query = "
            insert into accountsubsubgroup(
            subsubgroupcode
            ,subsubgroupnameeng
            ,subsubgroupnameuni
            ,groupcode
            ,subgroupcode
	        )
	        values (
            ".$this->invl($this->subsubgroupcode,true)."
            ,".$this->invl($this->subsubgroupnameeng,false)."
            ,".$this->invl($this->subsubgroupnameuni,false)."
            ,".$this->invl($this->groupcode,true,true)."
            ,".$this->invl($this->subgroupcode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->subsubgroupnameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subsubgroupnameeng like '%".$this->subsubgroupnameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and subsubgroupnameeng like '%".$this->subsubgroupnameeng."%'";
			       }
		    }
		    if ($this->subsubgroupnameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subsubgroupnameuni like '%".$this->subsubgroupnameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and subsubgroupnameuni like '%".$this->subsubgroupnameuni."%'";
			       }
		    }
            if ($this->groupcode!='' and $this->groupcode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="groupcode = ".$this->groupcode;
			       }
			       else
			       {
				         $cond=$cond." and groupcode = ".$this->groupcode;
			       }
		    }
		    if ($this->subgroupcode!='' and $this->subgroupcode>0)
		    {
			      if ($cond=='')
			      {
				        $cond="subgroupcode = ".$this->subgroupcode;
			       }
			       else
			       {
				         $cond=$cond." and subgroupcode = ".$this->subgroupcode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select * from accountsubsubgroup where ".$cond." order by subsubgroupcode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			      return $result;
		     }
		     else
		     {
		         $query = "select * from accountsubsubgroup order by subsubgroupcode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update accountsubsubgroup set 
        subsubgroupnameeng =".$this->invl($this->subsubgroupnameeng,false)."
        ,subsubgroupnameuni =".$this->invl($this->subsubgroupnameuni,false)."
        ,groupcode = ".$this->invl($this->groupcode,true)."
        ,subgroupcode = ".$this->invl($this->subgroupcode,true)."
        where 
        subsubgroupcode = ".$this->invl($this->subsubgroupcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from accountsubsubgroup
        where 
        subsubgroupcode = ".$this->invl($this->subsubgroupcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subsubgroupcode
        ,subsubgroupnameeng
        ,subsubgroupnameuni
        ,groupcode
        ,subgroupcode
        from accountsubsubgroup where 
        subsubgroupcode = ".$this->invl($this->subsubgroupcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subsubgroupcode = $row['SUBSUBGROUPCODE'];
                $this->subsubgroupnameeng = $row['SUBSUBGROUPNAMEENG'];
                $this->subsubgroupnameuni = $row['SUBSUBGROUPNAMEUNI'];
                $this->groupcode = $row['GROUPCODE'];
                $this->subgroupcode = $row['SUBGROUPCODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

