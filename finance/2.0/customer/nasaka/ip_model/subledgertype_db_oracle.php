<?php
//This is generated by Swapp Software Application on 10/11/2018 06:06:12 PM for PHP
include("../swappbase/formbase.php");
class subledgertype extends swappform
{
    Public $subledgertypecode;
    Public $subledgertypenameeng;
    Public $subledgertypenameuni;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->subledgertypecode='';
         $this->subledgertypenameeng='';
         $this->subledgertypenameuni='';
	   }
    private function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->subledgertypenameeng,'Sub ledger type name (English)');
        $this->checkrequired($this->subledgertypenameuni,'Sub ledger type name (Unicode)');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->subledgertypecode == '')
        {
            $query = "select nvl(max(subledgertypecode),0)+1 as subledgertypecode from subledgertype";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->subledgertypecode = $row["SUBLEDGERTYPECODE"];
        }
        $query = "
            insert into subledgertype(
            subledgertypecode
            ,subledgertypenameeng
            ,subledgertypenameuni
	        )
	        values (
            ".$this->invl($this->subledgertypecode,true)."
            ,".$this->invl($this->subledgertypenameeng,false)."
            ,".$this->invl($this->subledgertypenameuni,false)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->subledgertypenameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subledgertypenameeng like '%".$this->subledgertypenameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and subledgertypenameeng like '%".$this->subledgertypenameeng."%'";
			       }
		    }
		    if ($this->subledgertypenameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subledgertypenameuni like '%".$this->subledgertypenameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and subledgertypenameuni like '%".$this->subledgertypenameuni."%'";
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select * from subledgertype where ".$cond." order by subledgertypecode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			      return $result;
		     }
		     else
		     {
		         $query = "select * from subledgertype order by subledgertypecode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update subledgertype set 
        subledgertypenameeng =".$this->invl($this->subledgertypenameeng,false)."
        ,subledgertypenameuni =".$this->invl($this->subledgertypenameuni,false)."
        where 
        subledgertypecode = ".$this->invl($this->subledgertypecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from subledgertype
        where 
        subledgertypecode =".$this->invl($this->subledgertypecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subledgertypecode
        ,subledgertypenameeng
        ,subledgertypenameuni
        from SUBLEDGERTYPE where 
        subledgertypecode = ".$this->invl($this->subledgertypecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subledgertypecode = $row['SUBLEDGERTYPECODE'];
                $this->subledgertypenameeng = $row['SUBLEDGERTYPENAMEENG'];
                $this->subledgertypenameuni = $row['SUBLEDGERTYPENAMEUNI'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

