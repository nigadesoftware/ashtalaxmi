<?php
//This is generated by Swapp Software Application on 11/11/2018 11:16:11 AM for PHP
include("../swappbase/formbase.php");
class accountheaddetail extends swappform
{
    Public $accountcode;
    Public $subledgertypecode;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->accountcode='';
         $this->subledgertypecode='';
	   }
    private function entryvalidation()
    {
    		$this->start_validation();
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
            insert into accountheaddetail(
            accountcode
            ,subledgertypecode
	        )
	        values (
            ".$this->invl($this->accountcode,true,true)."
            ,".$this->invl($this->subledgertypecode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->accountcode!='' and $this->accountcode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.accountcode = ".$this->accountcode;
			       }
			       else
			       {
				         $cond=$cond." and t.accountcode = ".$this->accountcode;
			       }
		    }
		    if ($this->subledgertypecode!='' and $this->subledgertypecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.subledgertypecode = ".$this->subledgertypecode;
			       }
			       else
			       {
				         $cond=$cond." and t.subledgertypecode = ".$this->subledgertypecode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.accountcode,a.accountnameeng,a.accountnameuni,t.subledgertypecode 
                ,s.subledgertypenameeng,s.subledgertypenameuni
                from accountheaddetail t, accounthead a, subledgertype s where t.accountcode=a.accountcode and t.subledgertypecode=s.subledgertypecode and ".$cond." order by t.accountcode,t.subledgertypecode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.accountcode,a.accountnameeng,a.accountnameuni,t.subledgertypecode 
                ,s.subledgertypenameeng,s.subledgertypenameuni
                from accountheaddetail t, accounthead a, subledgertype s where t.accountcode=a.accountcode and t.subledgertypecode=s.subledgertypecode order by t.accountcode,t.subledgertypecode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update accountheaddetail set 
        where 
        accountcode = ".$this->invl($this->accountcode,true)."
        and subledgertypecode = ".$this->invl($this->subledgertypecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from accountheaddetail
        where 
        accountcode = ".$this->invl($this->accountcode,true)."
        and subledgertypecode = ".$this->invl($this->subledgertypecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        accountcode
        ,subledgertypecode
        from ACCOUNTHEADDETAIL where 
        accountcode = ".$this->invl($this->accountcode,true)."
        and subledgertypecode = ".$this->invl($this->subledgertypecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->accountcode = $row['ACCOUNTCODE'];
                $this->subledgertypecode = $row['SUBLEDGERTYPECODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

