<?php
//This is generated by Swapp Software Application on 11/11/2018 12:51:12 PM for PHP
include_once("../swappbase/formbase.php");
class accountsubledger extends swappform
{
    Public $subledgercode;
    Public $subledgernameeng;
    Public $subledgernameuni;
    Public $subledgertypecode;
    Public $accountcode;
    Public $referencecode;
    Public $referecenname;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->subledgercode='';
         $this->subledgernameeng='';
         $this->subledgernameuni='';
         $this->subledgertypecode='';
         $this->accountcode='';
         $this->referencecode='';
	   }
    private function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->subledgernameeng,'Sub ledger name (English)');
        $this->checkrequired($this->subledgernameuni,'Sub ledger name (Unicode)');
        $this->checkrequired($this->subledgertypecode,'Sub ledger type');
        if ($this->subledgertypecode>0)
        {
            if ($this->source() == 2)
            {
                $this->referencecode = strtoupper($this->referencecode);
                $this->checkrequired($this->referencecode,'Reference code');
            }
        }
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->subledgercode == '')
        {
            $query = "select nvl(max(subledgercode),100000)+1 as subledgercode 
            from accountsubledger
            where accountcode=".$this->accountcode;
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->subledgercode = $row["SUBLEDGERCODE"];
        }
        $query = "
            insert into accountsubledger(
            subledgercode
            ,subledgernameeng
            ,subledgernameuni
            ,subledgertypecode
            ,referencecode
            ,accountcode
	        )
	        values (
            ".$this->invl($this->subledgercode,true)."
            ,".$this->invl($this->subledgernameeng,false)."
            ,".$this->invl($this->subledgernameuni,false)."
            ,".$this->invl($this->subledgertypecode,true,true)."
            ,".$this->invl($this->referencecode,false)."
            ,".$this->invl($this->accountcode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->subledgercode!='' and $this->subledgercode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.subledgercode = ".$this->subledgercode;
			       }
			       else
			       {
				         $cond=$cond." and t.subledgercode = ".$this->subledgercode;
			       }
		    }
		    if ($this->subledgernameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.subledgernameeng like '%".$this->subledgernameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and t.subledgernameeng like '%".$this->subledgernameeng."%'";
			       }
		    }
		    if ($this->subledgernameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.subledgernameuni like '%".$this->subledgernameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and t.subledgernameuni like '%".$this->subledgernameuni."%'";
			       }
		    }
		    if ($this->subledgertypecode!='' and $this->subledgertypecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.subledgertypecode = ".$this->subledgertypecode;
			       }
			       else
			       {
				         $cond=$cond." and t.subledgertypecode = ".$this->subledgertypecode;
			       }
		    }
		    if ($this->referencecode!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.referencecode = '".$this->referencecode."'";
			       }
			       else
			       {
				         $cond=$cond." and t.referencecode = '".$this->referencecode."'";
			       }
		    }
		    if ($this->accountcode!='' and $this->accountcode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.accountcode = ".$this->accountcode;
			       }
			       else
			       {
				         $cond=$cond." and t.accountcode = ".$this->accountcode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from accountsubledger t where ".$cond." order by subledgercode,accountcode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from accountsubledger t order by subledgercode,accountcode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        //,referencecode = '".$this->invl($this->referencecode,true)."'
        $query = "
        update accountsubledger set 
        subledgernameeng =".$this->invl($this->subledgernameeng,false)."
        ,subledgernameuni =".$this->invl($this->subledgernameuni,false)."
        ,subledgertypecode = ".$this->invl($this->subledgertypecode,true)."
        where 
        subledgercode = ".$this->invl($this->subledgercode,true)."
        and accountcode = ".$this->invl($this->accountcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from accountsubledger
        where 
        subledgercode = ".$this->invl($this->subledgercode,true)."
        and accountcode = ".$this->invl($this->accountcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subledgercode
        ,subledgernameeng
        ,subledgernameuni
        ,subledgertypecode
        ,accountcode
        ,referencecode
        from accountsubledger where 
        subledgercode = ".$this->invl($this->subledgercode,true)."
         and accountcode = ".$this->invl($this->accountcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subledgercode = $row['SUBLEDGERCODE'];
                $this->subledgernameeng = $row['SUBLEDGERNAMEENG'];
                $this->subledgernameuni = $row['SUBLEDGERNAMEUNI'];
                $this->subledgertypecode = $row['SUBLEDGERTYPECODE'];
                $this->accountcode = $row['ACCOUNTCODE'];
                $this->referencecode = $row['REFERENCECODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
    public function fetchwithreferencename()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subledgercode
        ,subledgernameeng
        ,subledgernameuni
        ,subledgertypecode
        ,accountcode
        ,referencecode
        ,v.vvill_name_uni
        from accountsubledger where 
        from accountsubledger s,nst_nasaka_webpub.com_farmer_master f,nst_nasaka_webpub.com_village_master v
        where to_number(substr(s.referencecode,2,10))=f.nfarmer_code(+) and f.nvill_code=v.nvill_code and s.subledgertypecode=2
        and subledgercode = ".$this->invl($this->subledgercode,true)."
        and accountcode = ".$this->invl($this->accountcode,true)."
        ";

        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subledgercode = $row['SUBLEDGERCODE'];
                $this->subledgernameeng = $row['SUBLEDGERNAMEENG'];
                $this->subledgernameuni = $row['SUBLEDGERNAMEUNI'];
                $this->subledgertypecode = $row['SUBLEDGERTYPECODE'];
                $this->accountcode = $row['ACCOUNTCODE'];
                $this->referencecode = $row['REFERENCECODE'];
                $this->referencename = $row['VVILL_NAME_UNI'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
    public function fetchbyreference()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subledgercode
        ,subledgernameeng
        ,subledgernameuni
        ,subledgertypecode
        ,accountcode
        ,referencecode
        from nst_nasaka_finance.accountsubledger where 
        referencecode = ".$this->invl($this->referencecode,false)."
         and accountcode = ".$this->invl($this->accountcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subledgercode = $row['SUBLEDGERCODE'];
                $this->subledgernameeng = $row['SUBLEDGERNAMEENG'];
                $this->subledgernameuni = $row['SUBLEDGERNAMEUNI'];
                $this->subledgertypecode = $row['SUBLEDGERTYPECODE'];
                $this->accountcode = $row['ACCOUNTCODE'];
                $this->referencecode = $row['REFERENCECODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
    public function subopeningbalance($opdate)
    {   
        $query = "select sum(t.debit-t.credit) sopbl
        from vw_account_all_transaction t
        where t.cdate<'".$opdate."' 
        and t.yearperiodcode=".$_SESSION['yearperiodcode']."
        and t.accountcode=".$this->accountcode."
        and t.subledgercode=".$this->subledgercode;
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {  
            return $row['SOPBL'];
        }
        else
        {
        return 0;
        }
    } 
    public function subclosingbalance($cldate)
    {   
        $query = "select sum(t.debit-t.credit) clbal
        from vw_account_all_transaction t
        where t.cdate<='".$cldate."' 
        and t.yearperiodcode=".$_SESSION['yearperiodcode']."
        and t.accountcode=".$this->accountcode."
        and t.subledgercode=".$this->subledgercode;
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {  
            return $row['CLBAL'];
        }
        else
        {
        return 0;
        }
    }   

    public function subbdcredit($fdt,$tdt)
    {   
        $query = "select sum(t.credit) credit
        from vw_account_all_transaction t
        where t.cdate>='".$fdt."' 
        and t.cdate<='".$tdt."' 
        and t.yearperiodcode=".$_SESSION['yearperiodcode']."
        and t.accountcode=".$this->accountcode."
        and t.subledgercode=".$this->subledgercode;
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {  
            return $row['CREDIT'];
        }
        else
        {
        return 0;
        }
    }     

     public function subbddebit($fdt,$tdt)
    {   
        $query = "select sum(t.debit) debit
        from vw_account_all_transaction t
        where t.cdate>='".$fdt."' 
        and t.cdate<='".$tdt."' 
        and t.yearperiodcode=".$_SESSION['yearperiodcode']."
        and t.accountcode=".$this->accountcode."
        and t.subledgercode=".$this->subledgercode;
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {  
            return $row['DEBIT'];
        }
        else
        {
        return 0;
        }
    }

    public function source()
    {   
        $query = "select sourcecategorycode 
        from subledgertype t
        where subledgertypecode=".$this->subledgertypecode;
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {  
            return $row['SOURCECATEGORYCODE'];
        }
        else
        {
            return 0;
        }
    }
}
?>

