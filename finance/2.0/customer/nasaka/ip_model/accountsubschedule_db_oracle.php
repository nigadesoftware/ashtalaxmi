<?php
//This is generated by Swapp Software Application on 10/11/2018 06:06:11 PM for PHP
include("../swappbase/formbase.php");
class accountsubschedule extends swappform
{
    Public $subschedulecode;
    Public $subschedulenameeng;
    Public $subschedulenameuni;
    Public $schedulecode;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->subschedulecode='';
         $this->subschedulenameeng='';
         $this->subschedulenameuni='';
         $this->schedulecode='';
	   }
    private function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->subschedulenameeng,'Sub schedule name(English)');
        $this->checkrequired($this->subschedulenameuni,'Sub schedule code(Unicode)');
        $this->checkrequired($this->schedulecode,'Schedule Code');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->subschedulecode == '')
        {
            $query = "select nvl(max(subschedulecode),0)+1 as subschedulecode from accountsubschedule";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->subschedulecode = $row["SUBSCHEDULECODE"];
        }
        $query = "
            insert into accountsubschedule(
            subschedulecode
            ,subschedulenameeng
            ,subschedulenameuni
            ,schedulecode
	        )
	        values (
            ".$this->invl($this->subschedulecode,true)."
            ,".$this->invl($this->subschedulenameeng,false)."
            ,".$this->invl($this->subschedulenameuni,false)."
            ,".$this->invl($this->schedulecode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->subschedulenameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subschedulenameeng like '%".$this->subschedulenameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and subschedulenameeng like '%".$this->subschedulenameeng."%'";
			       }
		    }
		    if ($this->subschedulenameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subschedulenameuni like '%".$this->subschedulenameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and subschedulenameuni like '%".$this->subschedulenameuni."%'";
			       }
		    }
		    if ($this->schedulecode!='' and $this->schedulecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="schedulecode = ".$this->schedulecode;
			       }
			       else
			       {
				         $cond=$cond." and schedulecode = ".$this->schedulecode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select * from accountsubschedule where ".$cond." order by subschedulecode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			      return $result;
		     }
		     else
		     {
		         $query = "select * from accountsubschedule order by subschedulecode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update accountsubschedule set 
        subschedulenameeng =".$this->invl($this->subschedulenameeng,false)."
        ,subschedulenameuni =".$this->invl($this->subschedulenameuni,false)."
        ,schedulecode = ".$this->invl($this->schedulecode,true)."
        where 
        subschedulecode = ".$this->invl($this->subschedulecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from accountsubschedule
        where 
        subschedulecode = ".$this->invl($this->subschedulecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subschedulecode
        ,subschedulenameeng
        ,subschedulenameuni
        ,schedulecode
        from ACCOUNTSUBSCHEDULE where 
        subschedulecode = ".$this->invl($this->subschedulecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subschedulecode = $row['SUBSCHEDULECODE'];
                $this->subschedulenameeng = $row['SUBSCHEDULENAMEENG'];
                $this->subschedulenameuni = $row['SUBSCHEDULENAMEUNI'];
                $this->schedulecode = $row['SCHEDULECODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

