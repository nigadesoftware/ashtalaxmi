<?php
//This is generated by Swapp Software Application on 10/11/2018 06:06:11 PM for PHP
include("../swappbase/formbase.php");
class accountsubsubschedule extends swappform
{
    Public $subsubschedulecode;
    Public $subsubschedulenameeng;
    Public $subsubschedulenameuni;
    Public $schedulecode;
    Public $subschedulecode;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->subsubschedulecode='';
         $this->subsubschedulenameeng='';
         $this->subsubschedulenameuni='';
         $this->schedulecode='';
         $this->subschedulecode='';
	   }
    private function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->subsubschedulenameeng,'Sub sub schedule name(English)');
        $this->checkrequired($this->subsubschedulenameuni,'Sub sub schedule code(Unicode)');
        $this->checkrequired($this->schedulecode,'Schedule Code');
        $this->checkrequired($this->subschedulecode,'Sub schedule Code');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->subsubschedulecode == '')
        {
            $query = "select nvl(max(subsubschedulecode),0)+1 as subsubschedulecode from accountsubsubschedule";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->subsubschedulecode = $row["SUBSUBSCHEDULECODE"];
        }
        $query = "
            insert into accountsubsubschedule(
            subsubschedulecode
            ,subsubschedulenameeng
            ,subsubschedulenameuni
            ,schedulecode
            ,subschedulecode
	        )
	        values (
            ".$this->invl($this->subsubschedulecode,true)."
            ,".$this->invl($this->subsubschedulenameeng,false)."
            ,".$this->invl($this->subsubschedulenameuni,false)."
            ,".$this->invl($this->schedulecode,true,true)."
            ,".$this->invl($this->subschedulecode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->subsubschedulenameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subsubschedulenameeng like '%".$this->subsubschedulenameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and subsubschedulenameeng like '%".$this->subsubschedulenameeng."%'";
			       }
		    }
		    if ($this->subsubschedulenameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="subsubschedulenameuni like '%".$this->subsubschedulenameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and subsubschedulenameuni like '%".$this->subsubschedulenameuni."%'";
			       }
		    }
            if ($this->schedulecode!='' and $this->schedulecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="schedulecode = ".$this->schedulecode;
			       }
			       else
			       {
				         $cond=$cond." and schedulecode = ".$this->schedulecode;
			       }
		    }
            if ($this->subschedulecode!='' and $this->subschedulecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="subschedulecode = ".$this->subschedulecode;
			       }
			       else
			       {
				         $cond=$cond." and subschedulecode = ".$this->subschedulecode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select * from accountsubsubschedule where ".$cond." order by subsubschedulecode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			      return $result;
		     }
		     else
		     {
		         $query = "select * from accountsubsubschedule order by subsubschedulecode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update accountsubsubschedule set 
        subsubschedulenameeng =".$this->invl($this->subsubschedulenameeng,false)."
        ,subsubschedulenameuni =".$this->invl($this->subsubschedulenameuni,false)."
        ,schedulecode = ".$this->invl($this->schedulecode,true)."
        ,subschedulecode = ".$this->invl($this->subschedulecode,true)."
        where 
        subsubschedulecode = ".$this->invl($this->subsubschedulecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from accountsubsubschedule
        where 
        subsubschedulecode = ".$this->invl($this->subsubschedulecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        subsubschedulecode
        ,subsubschedulenameeng
        ,subsubschedulenameuni
        ,schedulecode
        ,subschedulecode
        from ACCOUNTSUBSUBSCHEDULE where 
        subsubschedulecode = ".$this->invl($this->subsubschedulecode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result,OCI_NO_AUTO_COMMIT);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->subsubschedulecode = $row['SUBSUBSCHEDULECODE'];
                $this->subsubschedulenameeng = $row['SUBSUBSCHEDULENAMEENG'];
                $this->subsubschedulenameuni = $row['SUBSUBSCHEDULENAMEUNI'];
                $this->schedulecode = $row['SCHEDULECODE'];
                $this->subschedulecode = $row['SUBSCHEDULECODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>