<?php
    //This is generated by Swapp Software Application on 04/05/2019 03:31:21 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select contractorcode,
       contractornameuni,
       contractornameeng
       from contractor where rownum <= 10";
   }
   else
   {
       $query = "select contractorcode,
       contractornameuni,
       contractornameeng
       from contractor where (upper(contractornameeng) like upper('%".$searchTerm."%')
       or contractornameuni like '%".$searchTerm."%'
       or to_char(contractorcode) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['CONTRACTORCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['CONTRACTORNAMEENG'];
       }
       else
       {
           $name = $row['CONTRACTORNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

