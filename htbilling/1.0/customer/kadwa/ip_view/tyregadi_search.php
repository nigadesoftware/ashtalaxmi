<?php
    //This is generated by Swapp Software Application on 18/05/2019 07:38:00 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select tyregadicode,
       tyregadinumber,
       tyregadinumber
       from nst_nasaka_kadwa.tyregadi where rownum <= 10";
   }
   else
   {
       $query = "select tyregadicode,
       tyregadinumber,
       tyregadinumber
       from nst_nasaka_kadwa.tyregadi where (upper(tyregadinumber) like upper('%".$searchTerm."%')
       or tyregadinumber like '%".$searchTerm."%'
       or to_char(tyregadicode) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['TYREGADICODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['TYREGADINUMBER'];
       }
       else
       {
           $name = $row['TYREGADINUMBER'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

