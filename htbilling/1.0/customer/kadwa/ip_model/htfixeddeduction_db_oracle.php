<?php
//This is generated by Swapp Software Application on 15/05/2019 07:41:28 PM for PHP
require_once("../swappbase/formbase.php");
class htfixeddeduction extends swappform
{
    Public $transactionnumber;
    Public $seasoncode;
    Public $billtypecode;
    Public $deductioncode;
    Public $deductionamount;
    Public $rulecode;
    public $servicetrhrcategorycode;

    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->transactionnumber='';
         $this->seasoncode='';
         $this->billtypecode='';
         $this->deductioncode='';
         $this->deductionamount='';
         $this->rulecode='';
         $this->servicetrhrcategorycode='';
	   }
    private function entryvalidation()
    {
    	$this->start_validation();
        $this->checkrequired($this->seasoncode,'Season');
        $this->checkrequired($this->billtypecode,'Bill Type');
        $this->checkrequired($this->deductioncode,'Deduction');
        $this->checkrequired($this->deductionamount,'Deduction Amount');
        $this->checkrequired($this->rulecode,'Rule Code');
        $this->checkrequired($this->servicetrhrcategorycode,'Service Code');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->transactionnumber == '')
        {
            $query = "select nvl(max(transactionnumber),0)+1 as transactionnumber from htfixeddeduction";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->transactionnumber = $row["TRANSACTIONNUMBER"];
        }
        $query = "
            insert into htfixeddeduction(
            transactionnumber
            ,seasoncode
            ,billtypecode
            ,deductioncode
            ,deductionamount
            ,rulecode
            ,servicetrhrcategorycode
	        )
	        values (
            ".$this->invl($this->transactionnumber,true)."
            ,".$this->invl($this->seasoncode,true)."
            ,".$this->invl($this->billtypecode,true,true)."
            ,".$this->invl($this->deductioncode,true,true)."
            ,".$this->invl($this->deductionamount,true)."
            ,".$this->invl($this->rulecode,true)."
            ,".$this->invl($this->servicetrhrcategorycode,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->seasoncode!='' and $this->seasoncode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.seasoncode = ".$this->seasoncode;
			       }
			       else
			       {
				         $cond=$cond." and t.seasoncode = ".$this->seasoncode;
			       }
		    }
		    if ($this->billtypecode!='' and $this->billtypecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.billtypecode = ".$this->billtypecode;
			       }
			       else
			       {
				         $cond=$cond." and t.billtypecode = ".$this->billtypecode;
			       }
		    }
		    if ($this->deductioncode!='' and $this->deductioncode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.deductioncode = ".$this->deductioncode;
			       }
			       else
			       {
				         $cond=$cond." and t.deductioncode = ".$this->deductioncode;
			       }
		    }
		    if ($this->deductionamount!='' and $this->deductionamount!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.deductionamount = ".$this->deductionamount;
			       }
			       else
			       {
				         $cond=$cond." and t.deductionamount = ".$this->deductionamount;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from htfixeddeduction t where ".$cond." order by transactionnumber";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from htfixeddeduction t order by transactionnumber";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update htfixeddeduction set 
        seasoncode = ".$this->invl($this->seasoncode,true)."
        ,billtypecode = ".$this->invl($this->billtypecode,true)."
        ,deductioncode = ".$this->invl($this->deductioncode,true)."
        ,deductionamount = ".$this->invl($this->deductionamount,true)."
        ,rulecode = ".$this->invl($this->rulecode,true)."
        ,servicetrhrcategorycode = ".$this->invl($this->servicetrhrcategorycode,true)."

        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from htfixeddeduction
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        transactionnumber
        ,seasoncode
        ,billtypecode
        ,deductioncode
        ,deductionamount
        ,rulecode
        ,servicetrhrcategorycode
        from htfixeddeduction where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->transactionnumber = $row['TRANSACTIONNUMBER'];
                $this->seasoncode = $row['SEASONCODE'];
                $this->billtypecode = $row['BILLTYPECODE'];
                $this->deductioncode = $row['DEDUCTIONCODE'];
                $this->deductionamount = $row['DEDUCTIONAMOUNT'];
                $this->rulecode = $row['RULECODE'];
                $this->servicetrhrcategorycode = $row['SERVICETRHRCATEGORYCODE'];
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

