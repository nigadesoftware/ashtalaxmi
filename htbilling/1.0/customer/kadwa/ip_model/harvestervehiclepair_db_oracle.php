<?php
//This is generated by Swapp Software Application on 07/05/2019 05:06:01 PM for PHP
require_once("../swappbase/formbase.php");
require_once("../ip_model/vehicle_db_oracle.php");
class harvestervehiclepair extends swappform
{
    Public $transactionnumber;
    Public $seasoncode;
    Public $harvestercode;
    Public $vehiclecode;
    Public $servicetrhrcategorycode;
    Public $vehicle;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->transactionnumber='';
         $this->seasoncode='';
         $this->harvestercode='';
         $this->vehiclecode='';
	}
    private function entryvalidation()
    {
    	$this->start_validation();
        $this->checkrequired($this->vehiclecode,'Vehicle');
        $this->end_validation();
        return $this->invalidid;
	}
    private function datavalidation()
    {
        $this->start_validation();
        if ($this->dataoperationmode == operationmode::Insert)
        {
            $query = "select count(*) as cnt from harvestervehiclepair where seasoncode=".$this->seasoncode.
            " and vehiclecode=".$this->vehiclecode;
        }
        else
        {
            $query = "select count(*) as cnt from harvestervehiclepair where seasoncode=".$this->seasoncode.
            " and vehiclecode=".$this->vehiclecode." and transactionnumber<>".$this->transactionnumber;
        }
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
            if ($row['CNT']>0)
            {
                $this->invalidid=-456;
                $this->invalidmessagetext='Vehicle already exists in pair';
            }
        }
        if ($this->dataoperationmode == operationmode::Insert)
        {
            $query = "select count(*) as cnt from harvestervehiclepair where seasoncode=".$this->seasoncode.
            " and harvestercode=".$this->harvestercode;
        }
        else
        {
            $query = "select count(*) as cnt from harvestervehiclepair where seasoncode=".$this->seasoncode.
            " and harvestercode=".$this->harvestercode." and transactionnumber<>".$this->transactionnumber;
        }
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
            if ($row['CNT']>0)
            {
                $this->invalidid=-456;
                $this->invalidmessagetext='Harvester already exists in pair';
            }
        }
        $this->end_validation();
        return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        if ($this->transactionnumber == '')
        {
            $query = "select nvl(max(transactionnumber),0)+1 as transactionnumber from harvestervehiclepair";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->transactionnumber = $row["TRANSACTIONNUMBER"];
        }
        $query = "
            insert into harvestervehiclepair(
            transactionnumber
            ,seasoncode
            ,harvestercode
            ,vehiclecode
	        )
	        values (
            ".$this->invl($this->transactionnumber,true)."
            ,".$this->invl($this->seasoncode,true)."
            ,".$this->invl($this->harvestercode,true)."
            ,".$this->invl($this->vehiclecode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->vehiclecode!='' and $this->vehiclecode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.vehiclecode = ".$this->vehiclecode;
			       }
			       else
			       {
				         $cond=$cond." and t.vehiclecode = ".$this->vehiclecode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from harvestervehiclepair t where ".$cond." order by transactionnumber";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from harvestervehiclepair t order by transactionnumber";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update harvestervehiclepair set 
        seasoncode = ".$this->invl($this->seasoncode,true)."
        ,harvestercode = ".$this->invl($this->harvestercode,true)."
        ,vehiclecode = ".$this->invl($this->vehiclecode,true)."
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from harvestervehiclepair
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        transactionnumber
        ,seasoncode
        ,harvestercode
        ,vehiclecode
        from harvestervehiclepair where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->transactionnumber = $row['TRANSACTIONNUMBER'];
                $this->seasoncode = $row['SEASONCODE'];
                $this->harvestercode = $row['HARVESTERCODE'];
                $this->vehiclecode = $row['VEHICLECODE'];
                $vehicle1 = new vehicle($this->connection);
                $vehicle1->vehiclecode = $this->vehiclecode;
                $vehicle1->seasoncode = $this->seasoncode;
                $vehicle1->fetch();
                $this->vehicle = $vehicle1->vehiclenumber;
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
    public function fetchall($seasoncode,$harvestercode)
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        transactionnumber
        ,p.seasoncode
        ,harvestercode
        ,p.vehiclecode
        ,v.vehiclenumber
        from harvestervehiclepair p,vehicle v  
        where p.vehiclecode =v.vehiclecode
        and p.seasoncode=v.seasoncode 
        and p.seasoncode = ".$this->invl($seasoncode,true)."
        and harvestercode = ".$this->invl($harvestercode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        return $result;
    }

}
?>

