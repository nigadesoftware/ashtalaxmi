<?php
    //This is generated by Swapp Software Application on 21/10/2018 10:54:45 AM for PHP
    require("../info/phpsqlajax_dbinfo.php");
    require("../info/phpgetloginview.php");
    require("../info/routine.php");
    $connection =petrolpump_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select customercode,
       customernameuni,
       customernameeng,vehiclenumber,refcode
       from customer where rownum <= 100
       and seasoncode is null
       order by customernameeng";
   }
   else
   {
       $query = "select customercode,
       customernameuni,
       customernameeng,vehiclenumber,refcode
       from customer where (upper(customernameeng) like upper('%".$searchTerm."%')
       or customernameuni like '%".$searchTerm."%'
       or to_char(refcode) like '".$searchTerm."' 
       or upper(vehiclenumber) like upper('%".$searchTerm."%')) 
       and rownum <= 100 and seasoncode is null
       order by customernameeng";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['CUSTOMERCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['CUSTOMERNAMEENG'].' '.$row['VEHICLENUMBER'];
       }
       else
       {
           $name = $row['CUSTOMERNAMEUNI'].' '.$row['VEHICLENUMBER'];
       }
       $vehiclenumber = $row['VEHICLENUMBER'];
       $refcode = $row['REFCODE'];
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           ,'refcode' => $refcode
           ,'vehiclenumber' => $vehiclenumber
           );
   }
   //return json data
   echo json_encode($data);
?>
