<?php
    //This is generated by Swapp Software Application on 02/05/2019 12:10:18 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select bankcode,
       banknameuni,
       banknameeng,
       shortname
       from bank r";
   }
   else
   {
       $query = "select bankcode,
       banknameuni,
       banknameeng,
       shortname
       from bank r
       where (upper(banknameeng) like upper('%".$searchTerm."%')
       or banknameuni like '%".$searchTerm."%'
       or to_char(bankcode) like '".$searchTerm."'
       or shortname like '%".strtoupper($searchTerm)."%')";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['BANKCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['SHORTNAME'].', '.$row['BANKNAMEENG'];
       }
       else
       {
           $name = $row['SHORTNAME'].', '.$row['BANKNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

