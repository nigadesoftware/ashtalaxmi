<?php
    //This is generated by Swapp Software Application on 26/05/2019 05:37:52 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<1) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select districtcode,
       districtnameuni,
       districtnameeng
       from district";
   }
   else
   {
       $query = "select districtcode,
       districtnameuni,
       districtnameeng
       from district where (upper(districtnameeng) like upper('%".$searchTerm."%')
       or districtnameuni like '%".$searchTerm."%'
       or to_char(districtcode) like '".$searchTerm."')";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['DISTRICTCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['DISTRICTNAMEENG'];
       }
       else
       {
           $name = $row['DISTRICTNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

