<?php
    //This is generated by Swapp Software Application on 14/05/2020 10:35:55 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<1 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select PUMPCODE,
       PUMPNAMEUNI,
       PUMPNAMEENG
       from PUMP where rownum <= 100";
   }
   else
   {
       $query = "select PUMPCODE,
       PUMPNAMEUNI,
       PUMPNAMEENG
       from PUMP where (upper(PUMPNAMEENG) like upper('%".$searchTerm."%')
       or PUMPNAMEUNI like '%".$searchTerm."%'
       or to_char(PUMPCODE) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['PUMPCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['PUMPNAMEENG'];
       }
       else
       {
           $name = $row['PUMPNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

