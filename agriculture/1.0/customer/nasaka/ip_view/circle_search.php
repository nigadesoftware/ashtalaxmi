<?php
    //This is generated by Swapp Software Application on 27/05/2019 11:16:03 AM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (isset($_GET['divisioncode']))
    {
        $divisioncode=$_GET['divisioncode'];
    }
    else
    {
        $divisioncode=0;
    }
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       if ($divisioncode==0)
       {
            $query = "select circlecode,
            circlenameuni,
            circlenameeng
            from circle";
       }
       else
       {    
            $query = "select circlecode,
            circlenameuni,
            circlenameeng
            from circle where divisioncode=".$divisioncode;
       }
   }
   else
   {
       if ($divisioncode==0)
       {
            $query = "select circlecode,
            circlenameuni,
            circlenameeng
            from circle where (upper(circlenameeng) like upper('%".$searchTerm."%')
            or circlenameuni like '%".$searchTerm."%'
            or to_char(circlecode) like '".$searchTerm."')";
       }
       else
       {
            $query = "select circlecode,
            circlenameuni,
            circlenameeng
            from circle where divisioncode=".$divisioncode." and (upper(circlenameeng) like upper('%".$searchTerm."%')
            or circlenameuni like '%".$searchTerm."%'
            or to_char(circlecode) like '".$searchTerm."')";
       }
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['CIRCLECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['CIRCLENAMEENG'];
       }
       else
       {
           $name = $row['CIRCLENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

