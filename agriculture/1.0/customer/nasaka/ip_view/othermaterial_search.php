<?php
    //This is generated by Swapp Software Application on 17/08/2020 06:43:32 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $payeecategorycode = 1;
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select OTHMATCATEGORYCODE,
       OTHMATCATEGORYNAMEUNI,
       OTHMATCATEGORYNAMEENG
       from OTHERMATERIAL where rownum <= 100";
   }
   else
   {
       $query = "select OTHMATCATEGORYCODE,
       OTHMATCATEGORYNAMEUNI,
       OTHMATCATEGORYNAMEENG
       from OTHERMATERIAL where (upper(OTHMATCATEGORYNAMEENG) like upper('%".$searchTerm."%')
       or OTHMATCATEGORYNAMEUNI like '%".$searchTerm."%'
       or to_char(OTHMATCATEGORYCODE) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['OTHMATCATEGORYCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['OTHMATCATEGORYNAMEENG'];
       }
       else
       {
           $name = $row['OTHMATCATEGORYNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

