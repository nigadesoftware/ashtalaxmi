<?php
    //This is generated by Swapp Software Application on 28/05/2019 07:19:54 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $villagecode = $_GET['villagecode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select subvillagecode,
       subvillagenameuni,
       subvillagenameeng
       from subvillage
       where villagecode=".$villagecode;
   }
   else
   {
       $query = "select subvillagecode,
       subvillagenameuni,
       subvillagenameeng
       from subvillage where (upper(subvillagenameeng) like upper('%".$searchTerm."%')
       or subvillagenameuni like '%".$searchTerm."%'
       or to_char(subvillagecode) like '".$searchTerm."') and villagecode=".$villagecode;
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['SUBVILLAGECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['SUBVILLAGENAMEENG'];
       }
       else
       {
           $name = $row['SUBVILLAGENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>