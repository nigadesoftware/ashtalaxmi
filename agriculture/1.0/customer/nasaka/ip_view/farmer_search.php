<?php
    //This is generated by Swapp Software Application on 28/05/2019 07:19:54 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection = swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select farmercode,
       farmernameuni,
       farmernameeng,
       c.farmercategorycode,
       c.farmercategorynameuni,
       c.farmercategorynameeng,
       v.villagecode,
       v.villagenameuni,
       v.villagenameeng,
       f.mobilenumber,
       f.adharnumber,
       r.circlenameeng,
       r.circlenameuni
       from farmer f,farmercategory c,village v ,circle r 
       where f.farmercategorycode=c.farmercategorycode
       and f.villagecode=v.villagecode
       and v.circlecode=r.circlecode
       and rownum <= 100";
   }
   else
   {
       $query = "select farmercode,
       farmernameuni,
       farmernameeng,
       c.farmercategorycode,
       c.farmercategorynameuni,
       c.farmercategorynameeng,
       v.villagecode,
       v.villagenameuni,
       v.villagenameeng,
       f.mobilenumber,
       f.adharnumber,
       r.circlenameeng,
       r.circlenameuni
       from farmer f,farmercategory c,village v,circle r 
       where f.farmercategorycode=c.farmercategorycode
       and f.villagecode=v.villagecode
       and v.circlecode=r.circlecode
       and (upper(farmernameeng) like upper('%".$searchTerm."%')
       or farmernameuni like '%".$searchTerm."%'
       or to_char(farmercode) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['FARMERCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['FARMERNAMEENG'];
           $farmercategoryname=$row['FARMERCATEGORYNAMEENG'];
           $villagename=$row['VILLAGENAMEENG'];
           $circlename=$row['CIRCLENAMEENG'];
       }
       else
       {
           $name = $row['FARMERNAMEUNI'];
           $farmercategoryname=$row['FARMERCATEGORYNAMEUNI'];
           $villagename=$row['VILLAGENAMEUNI'];
           $circlename=$row['CIRCLENAMEUNI'];
       }
       $farmercategorycode=$row['FARMERCATEGORYCODE'];
       $villagecode=$row['VILLAGECODE'];
       $mobilenumber=$row['MOBILENUMBER'];
       $adharnumber=$row['ADHARNUMBER'];
       $data[] = array( 
           'id' => $id
           ,'label' => $name.' - '.$farmercategoryname.' - '.$villagename.' - '.$circlename
           ,'value' => $name
           ,'farmercategorycode' => $farmercategorycode
           ,'farmercategoryname' => $farmercategoryname
           ,'villagecode' => $villagecode
           ,'villagename' => $villagename
           ,'mobilenumber' => $mobilenumber
           ,'adharnumber' => $adharnumber
           );
   }
   //return json data
   echo json_encode($data);
?>