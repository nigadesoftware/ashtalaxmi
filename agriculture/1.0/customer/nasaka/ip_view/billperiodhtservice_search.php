<?php
    //This is generated by Swapp Software Application on 17/08/2020 06:43:32 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $payeecategorycode = $_GET['payeecategorycode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
        $query = "select htserviceCODE,
                htserviceNAMEUNI,
                htserviceNAMEENG
                from htservice where 
                rownum <= 100";
   }
   else
   {
        $query = "select htserviceCODE,
        htserviceNAMEUNI,
        htserviceNAMEENG
        from htservice where 
        htservicecategorycode=1
        and payeecategorycode=".$payeecategorycode."
        and (upper(htserviceNAMEENG) like upper('%".$searchTerm."%')
        or htserviceNAMEUNI like '%".$searchTerm."%'
        or to_char(htserviceCODE) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); 
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['HTSERVICECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['HTSERVICENAMEENG'];
       }
       else
       {
           $name = $row['HTSERVICENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

