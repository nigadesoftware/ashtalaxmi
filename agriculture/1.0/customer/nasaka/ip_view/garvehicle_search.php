<?php
    //This is generated by Swapp Software Application on 07/05/2019 05:06:02 PM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $seasoncode = $_GET['seasoncode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select v.vehiclecode,v.vehiclenumber 
        from (
        select vehiclecode
        from vehicle where seasoncode=".$seasoncode." 
        minus
        select t.vehiclecode
        from vehicle t 
        where (t.seasoncode,t.vehiclecode) in 
        (select p.seasoncode,p.vehiclecode1
        from transporterguarantor p
        where t.seasoncode=".$seasoncode." 
        union all
        select p.seasoncode,p.vehiclecode2
        from transporterguarantor p
        where t.seasoncode=".$seasoncode." 
        union all
        select p.seasoncode,p.vehiclecode3
        from transporterguarantor p
        where t.seasoncode=".$seasoncode." 
        ))t, vehicle v 
        where t.vehiclecode=v.vehiclecode
        and v.seasoncode=".$seasoncode;
   }
   else
   {
       $query = "select v.vehiclecode,v.vehiclenumber 
        from (
        select vehiclecode
        from vehicle where seasoncode=".$seasoncode." 
        minus
        select t.vehiclecode
        from vehicle t 
        where (t.seasoncode,t.vehiclecode) in 
        (select p.seasoncode,p.vehiclecode1
        from transporterguarantor p
        where t.seasoncode=".$seasoncode." 
        union all
        select p.seasoncode,p.vehiclecode2
        from transporterguarantor p
        where t.seasoncode=".$seasoncode." 
        union all
        select p.seasoncode,p.vehiclecode3
        from transporterguarantor p
        where t.seasoncode=".$seasoncode." 
        ))t, vehicle v 
        where t.vehiclecode=v.vehiclecode
        and v.seasoncode=".$seasoncode." 
        and (upper(vehiclenumber) like upper('%".$searchTerm."%')
       or vehiclenumber like '%".$searchTerm."%'
       or to_char(v.vehiclecode) like '".$searchTerm."') and v.seasoncode=".$seasoncode;
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['VEHICLECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['VEHICLENUMBER'];
       }
       else
       {
           $name = $row['VEHICLENUMBER'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>