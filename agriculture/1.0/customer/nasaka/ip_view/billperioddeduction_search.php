<?php
    //This is generated by Swapp Software Application on 17/08/2020 06:43:32 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $payeecategorycode = $_GET['payeecategorycode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
        //for farmer only factory deductions   
        if ($payeecategorycode==1)
        {
                $query = "select DEDUCTIONCODE,
                DEDUCTIONNAMEUNI,
                DEDUCTIONNAMEENG
                from DEDUCTION where 
                payeecategorycode=".$payeecategorycode." 
                and rownum <= 100";
        }
        //for ht only factory deductions   
        elseif ($payeecategorycode==2)
        {
                $query = "select DEDUCTIONCODE,
                DEDUCTIONNAMEUNI,
                DEDUCTIONNAMEENG
                from DEDUCTION where 
                payeecategorycode=".$payeecategorycode." 
                and rownum <= 100";
        }
        else
        {
                $query = "select DEDUCTIONCODE,
                DEDUCTIONNAMEUNI,
                DEDUCTIONNAMEENG
                from DEDUCTION where 
                payeecategorycode=".$payeecategorycode." 
                and rownum <= 100";
        }
   }
   else
   {
       if ($payeecategorycode==1)
       {
            $query = "select DEDUCTIONCODE,
            DEDUCTIONNAMEUNI,
            DEDUCTIONNAMEENG
            from DEDUCTION where 
            deductioncategorycode=1
            and payeecategorycode=".$payeecategorycode."
            and (upper(DEDUCTIONNAMEENG) like upper('%".$searchTerm."%')
            or DEDUCTIONNAMEUNI like '%".$searchTerm."%'
            or to_char(DEDUCTIONCODE) like '".$searchTerm."') and rownum <= 100";
       }
       else
       {
            $query = "select DEDUCTIONCODE,
            DEDUCTIONNAMEUNI,
            DEDUCTIONNAMEENG
            from DEDUCTION where 
            payeecategorycode=".$payeecategorycode."
            and (upper(DEDUCTIONNAMEENG) like upper('%".$searchTerm."%')
            or DEDUCTIONNAMEUNI like '%".$searchTerm."%'
            or to_char(DEDUCTIONCODE) like '".$searchTerm."') and rownum <= 100";
       }
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['DEDUCTIONCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['DEDUCTIONNAMEENG'];
       }
       else
       {
           $name = $row['DEDUCTIONNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

