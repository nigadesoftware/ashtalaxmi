<?php
    //This is generated by Swapp Software Application on 27/05/2019 11:16:03 AM for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif ((strlen($searchTerm)<3) and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select talukacode,
       talukanameuni,
       talukanameeng
       from taluka";
   }
   else
   {
       $query = "select talukacode,
       talukanameuni,
       talukanameeng
       from taluka where (upper(talukanameeng) like upper('%".$searchTerm."%')
       or talukanameuni like '%".$searchTerm."%'
       or to_char(talukacode) like '".$searchTerm."')";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['TALUKACODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['TALUKANAMEENG'];
       }
       else
       {
           $name = $row['TALUKANAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

