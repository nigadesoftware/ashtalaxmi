<?php
//This is generated by Swapp Software Application on 26/05/2019 05:37:51 PM for PHP
require_once("../swappbase/formbase.php");
require_once("../ip_model/district_db_oracle.php");
class taluka extends swappform
{
    Public $talukacode;
    Public $talukanameeng;
    Public $talukanameuni;
    Public $districtcode;
    Public $district;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->talukacode='';
         $this->talukanameeng='';
         $this->talukanameuni='';
         $this->districtcode='';
	   }
    public function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->talukanameeng,'Taluka Name (English)');
        $this->checkrequired($this->talukanameuni,'Taluka Name (Unicode)');
        $this->checkrequired($this->districtcode,'District');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->talukacode == '')
        {
            $query = "select nvl(max(talukacode),0)+1 as talukacode from taluka";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->talukacode = $row["TALUKACODE"];
        }
        $query = "
            insert into taluka(
            talukacode
            ,talukanameeng
            ,talukanameuni
            ,districtcode
	        )
	        values (
            ".$this->invl($this->talukacode,true)."
            ,".$this->invl($this->talukanameeng,false)."
            ,".$this->invl($this->talukanameuni,false)."
            ,".$this->invl($this->districtcode,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->talukacode!='' and $this->talukacode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.talukacode = ".$this->talukacode;
			       }
			       else
			       {
				         $cond=$cond." and t.talukacode = ".$this->talukacode;
			       }
		    }
		    if ($this->talukanameeng!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.talukanameeng like '%".$this->talukanameeng."%'";
			       }
			       else
			       {
				         $cond=$cond." and t.talukanameeng like '%".$this->talukanameeng."%'";
			       }
		    }
		    if ($this->talukanameuni!='')
		    {
			      if ($cond=='')
			      {
				        $cond="t.talukanameuni like '%".$this->talukanameuni."%'";
			       }
			       else
			       {
				         $cond=$cond." and t.talukanameuni like '%".$this->talukanameuni."%'";
			       }
		    }
		    if ($this->districtcode!='' and $this->districtcode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.districtcode = ".$this->districtcode;
			       }
			       else
			       {
				         $cond=$cond." and t.districtcode = ".$this->districtcode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from taluka t where ".$cond." order by talukacode";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from taluka t order by talukacode";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update taluka set 
        talukanameeng =".$this->invl($this->talukanameeng,false)."
        ,talukanameuni =".$this->invl($this->talukanameuni,false)."
        ,districtcode = ".$this->invl($this->districtcode,true)."
        where 
        talukacode = ".$this->invl($this->talukacode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from taluka
        where 
        talukacode = ".$this->invl($this->talukacode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        talukacode
        ,talukanameeng
        ,talukanameuni
        ,districtcode
        from taluka where 
        talukacode = ".$this->invl($this->talukacode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->talukacode = $row['TALUKACODE'];
                $this->talukanameeng = $row['TALUKANAMEENG'];
                $this->talukanameuni = $row['TALUKANAMEUNI'];
                $this->districtcode = $row['DISTRICTCODE'];
                $district1 = new district($this->connection);
                $district1->districtcode = $this->districtcode;
                $district1->fetch();
                if ($_SESSION['lng']=="English")
                {
                    $this->district = $district1->districtnameeng;
                }
                else
                {
                    $this->district = $district1->districtnameuni;
                }
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
    public function fetchall($districtcode)
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        talukacode
        ,talukanameeng
        ,talukanameuni
        ,districtcode
        from taluka where 
        districtcode = ".$this->invl($districtcode,true)."
        ";
        $result = oci_parse($this->connection, $query);
        return $result;
    }
}
?>

