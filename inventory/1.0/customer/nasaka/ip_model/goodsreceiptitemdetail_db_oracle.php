<?php
//This is generated by Nigade Software Technologies (opc) Private Limited 12/12/2020 10:18:33 am for PHP
require_once("../swappbase/formbase.php");
class goodsreceiptitemdetail extends swappform
{
    Public $transactionnumber;
    Public $serialnumber;
    Public $itemcode;
    Public $itemcodetextcombo;
    Public $quantity;
    Public $acceptedquantity;
    Public $poquantity;
    Public $pobalancequantity;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->transactionnumber='';
         $this->transactionnumbertextcombo='';
         $this->serialnumber='';
         $this->serialnumbertextcombo='';
         $this->itemcode='';
         $this->itemcodetextcombo='';
         $this->quantity='';
         $this->quantitytextcombo='';
	   }
    public function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->itemcode,'Item Code ','आयटम कोड');
        $this->checkrequired($this->quantity,'Quantity ','संख्या');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
        if ($this->acceptedquantity!='')
        {
            if ($this->acceptedquantity>$this->quantity)
            {
                $this->invalidid=-200;
    		    $this->invalidmessagetext='Accepted Quantity should not more than GRN Quantity';
            }
            else 
            {
                $this->invalidid=0;
                $this->invalidmessagetext='Validated';
            }
        }
        else 
        {
            $this->invalidid=0;
    		$this->invalidmessagetext='Validated';
        }
    		
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->transactionnumber == '')
        {
            $query = "select nvl(max(transactionnumber),0)+1 as transactionnumber from goodsreceiptitemdetail";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->transactionnumber = $row["TRANSACTIONNUMBER"];
        }
	    if ($this->serialnumber == '')
        {
            $query = "select nvl(max(serialnumber),0)+1 as serialnumber from goodsreceiptitemdetail";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->serialnumber = $row["SERIALNUMBER"];
        }
        $query = "
            insert into goodsreceiptitemdetail(
            transactionnumber
            ,serialnumber
            ,itemcode
            ,quantity
            ,acceptedquantity
	        )
	        values (
            ".$this->invl($this->transactionnumber,true)."
            ,".$this->invl($this->serialnumber,true)."
            ,".$this->invl($this->itemcode,true,true)."
            ,".$this->invl($this->quantity,true)."
            ,".$this->invl($this->acceptedquantity,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->transactionnumber!='' and $this->transactionnumber!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.transactionnumber = ".$this->transactionnumber;
			       }
			       else
			       {
				         $cond=$cond." and t.transactionnumber = ".$this->transactionnumber;
			       }
		    }
		    if ($this->itemcode!='' and $this->itemcode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.itemcode = ".$this->itemcode;
			       }
			       else
			       {
				         $cond=$cond." and t.itemcode = ".$this->itemcode;
			       }
		    }
            if ($this->quantity!='' and $this->quantity!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.quantity = ".$this->quantity;
			       }
			       else
			       {
				         $cond=$cond." and t.quantity = ".$this->quantity;
			       }
		    }
            if ($this->acceptedquantity!='' and $this->acceptedquantity!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.acceptedquantity = ".$this->acceptedquantity;
			       }
			       else
			       {
				         $cond=$cond." and t.acceptedquantity = ".$this->acceptedquantity;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from goodsreceiptitemdetail t where ".$cond." order by transactionnumber,serialnumber";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from goodsreceiptitemdetail t order by transactionnumber,serialnumber";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update goodsreceiptitemdetail set 
        itemcode = ".$this->invl($this->itemcode,true,true)."
        ,quantity = ".$this->invl($this->quantity,true,true)."
        ,acceptedquantity = ".$this->invl($this->acceptedquantity,true,true)."
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
         and serialnumber = ".$this->invl($this->serialnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from goodsreceiptitemdetail
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
         and serialnumber = ".$this->invl($this->serialnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        transactionnumber
        ,serialnumber
        ,itemcode
        ,quantity
        ,acceptedquantity
        from goodsreceiptitemdetail where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
         and serialnumber = ".$this->invl($this->serialnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->transactionnumber = $row['TRANSACTIONNUMBER'];
                $this->serialnumber = $row['SERIALNUMBER'];
                $this->itemcode = $row['ITEMCODE'];
                $this->quantity = $row['QUANTITY'];
                $this->acceptedquantity = $row['ACCEPTEDQUANTITY'];

                $query1 ="select i.ITEMCODE,
                ITEMNAMEENG,d.quantity,
                (select nvl(sum(quantity),0) quantity
                from goodsreceiptheader h,goodsreceiptitemdetail dd
                where h.transactionnumber=dd.transactionnumber
                and h.purchaseordertransnumber=h.transactionnumber and  dd.itemcode=d.itemcode
                and h.transactionnumber<>{$row['TRANSACTIONNUMBER']}
                and dd.serialnumber<>{$row['SERIALNUMBER']}
                ) grnquantity
                from ITEM i,purchesorderheader h,purchesorderitemdetail d 
                where h.transactionnumber=d.transactionnumber
                and i.itemcode =d.itemcode
                and d.itemcode={$row['ITEMCODE']}
                and h.transactionnumber=(select purchaseordertransnumber
                from goodsreceiptheader where transactionnumber=".$row['TRANSACTIONNUMBER'].")";
                $result1 = oci_parse($this->connection, $query1);
                $r1 = oci_execute($result1);
                if ($row1 = oci_fetch_array($result1,OCI_ASSOC+OCI_RETURN_NULLS))
                {
                    $this->poquantity = $row1['QUANTITY'];
                    $this->pobalancequantity = $row1['QUANTITY']-$row1['GRNQUANTITY']; 
                }
                $this->itemcodetextcombo = $this->textcomboname($connection=$this->connection,$table='ITEM',$datafield='ITEMCODE',$datafieldvalue=$row['ITEMCODE'],$engfield='ITEMNAMEENG',$unicodefield='ITEMNAMEENG');
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

