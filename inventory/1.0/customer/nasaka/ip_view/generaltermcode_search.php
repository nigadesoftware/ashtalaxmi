<?php
    //This is generated by Swapp Software Application on 06/09/2022 11:18:21 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select GENERALTERMCODE,
       GENERALTERMNAMEUNI,
       GENERALTERMNAMEENG
       from GENERALTERM where rownum <= 10";
   }
   else
   {
       $query = "select GENERALTERMCODE,
       GENERALTERMNAMEUNI,
       GENERALTERMNAMEENG
       from GENERALTERMCODE where (upper(GENERALTERMNAMEENG) like upper('%".$searchTerm."%')
       or GENERALTERMCODENAMEUNI like '%".$searchTerm."%'
       or to_char(GENERALTERMCODE) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['GENERALTERMCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['GENERALTERMNAMEENG'];
       }
       else
       {
           $name = $row['GENERALTERMNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

