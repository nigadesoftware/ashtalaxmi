<?php
    //This is generated by Swapp Software Application on 12/12/2020 10:07:53 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $suppliercode = $_GET['suppliercode'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<1 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select transactionnumber,
       goodsreceiptnotenumber
       from goodsreceiptheader 
       where financialyear=".$_SESSION['yearperiodcode']." 
       and suppliercode = ".$suppliercode." and rownum <= 100";
   }
   else
   {
       $query = "select transactionnumber,
       goodsreceiptnotenumber
       from goodsreceiptheader where suppliercode = ".$suppliercode." and financialyear=".$_SESSION['yearperiodcode']." and (upper(goodsreceiptnotenumber) like upper('%".$searchTerm."%')
       or goodsreceiptnotenumber like '%".$searchTerm."%'
       or to_char(transactionnumber) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['TRANSACTIONNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['GOODSRECEIPTNOTENUMBER'];
       }
       else
       {
           $name = $row['GOODSRECEIPTNOTENUMBER'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>