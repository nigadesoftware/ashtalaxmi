<?php
    //This is generated by Swapp Software Application on 12/12/2020 10:07:02 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select MAINSTORECODE,
       MAINSTORENAMEUNI,
       MAINSTORENAMEENG
       from MAINSTORE where rownum <= 100";
   }
   else
   {
       $query = "select MAINSTORECODE,
       MAINSTORENAMEUNI,
       MAINSTORENAMEENG
       from MAINSTORE where (upper(MAINSTORENAMEENG) like upper('%".$searchTerm."%')
       or MAINSTORENAMEUNI like '%".$searchTerm."%'
       or to_char(MAINSTORECODE) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['MAINSTORECODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['MAINSTORENAMEENG'];
       }
       else
       {
           $name = $row['MAINSTORENAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

