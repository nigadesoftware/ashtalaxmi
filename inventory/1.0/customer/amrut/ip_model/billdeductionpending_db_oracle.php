<?php
//This is generated by Nigade Software Technologies (opc) Private Limited 27/11/2022 03:13:52 pm for PHP
require_once("../swappbase/formbase.php");
class billdeductionpending extends swappform
{
    Public $transactionnumber;
    Public $transactiondate;
    Public $billperiodtransnumber;
    Public $billperiodtransnumbertextcombo;
    Public $farmercode;
    Public $farmercodetextcombo;
    Public $deductioncode;
    Public $deductioncodetextcombo;
    Public $pendingamount;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->transactionnumber='';
         $this->transactionnumbertextcombo='';
         $this->transactiondate='';
         $this->transactiondatetextcombo='';
         $this->billperiodtransnumber='';
         $this->billperiodtransnumbertextcombo='';
         $this->farmercode='';
         $this->farmercodetextcombo='';
         $this->deductioncode='';
         $this->deductioncodetextcombo='';
         $this->pendingamount='';
         $this->pendingamounttextcombo='';
	   }
    public function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->transactiondate,'Transaction Date','व्यवहार दिनांक');
        $this->checkrequired($this->billperiodtransnumber,'FortNight','फोर्टनाईट');
        $this->checkrequired($this->farmercode,'Farmer Code','सभासद कोड');
        $this->checkrequired($this->deductioncode,'Deduction Code','कपात कोड');
        $this->checkrequired($this->pendingamount,'Amount','रक्कम');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->transactionnumber == '')
        {
            $query = "select nvl(max(transactionnumber),0)+1 as transactionnumber from billdeductionpending";
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->transactionnumber = $row["TRANSACTIONNUMBER"];
        }
        if ($this->transactiondate!='')
        {
            $this->transactiondate = DateTime::createFromFormat('d/m/Y',$this->transactiondate)->format('d-M-Y');
        }
        $query = "
            insert into billdeductionpending(
            transactionnumber
            ,transactiondate
            ,billperiodtransnumber
            ,farmercode
            ,deductioncode
            ,pendingamount
	        )
	        values (
            ".$this->invl($this->transactionnumber,true)."
            ,".$this->invl($this->transactiondate,false)."
            ,".$this->invl($this->billperiodtransnumber,true,true)."
            ,".$this->invl($this->farmercode,true,true)."
            ,".$this->invl($this->deductioncode,true,true)."
            ,".$this->invl($this->pendingamount,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->transactionnumber!='' and $this->transactionnumber!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.transactionnumber = ".$this->transactionnumber;
			       }
			       else
			       {
				         $cond=$cond." and t.transactionnumber = ".$this->transactionnumber;
			       }
		    }
		    if ($this->transactiondate!='')
		    {
			      $this->transactiondate = DateTime::createFromFormat('d/m/Y',$this->transactiondate)->format('d-M-Y');
			      if ($cond=='')
			      {
				        $cond="t.transactiondate = '".$this->transactiondate."'";
			       }
			       else
			       {
				         $cond=$cond." and t.transactiondate = '".$this->transactiondate."'";
			       }
		    }
		    if ($this->billperiodtransnumber!='' and $this->billperiodtransnumber!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.billperiodtransnumber = ".$this->billperiodtransnumber;
			       }
			       else
			       {
				         $cond=$cond." and t.billperiodtransnumber = ".$this->billperiodtransnumber;
			       }
		    }
		    if ($this->farmercode!='' and $this->farmercode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.farmercode = ".$this->farmercode;
			       }
			       else
			       {
				         $cond=$cond." and t.farmercode = ".$this->farmercode;
			       }
		    }
		    if ($this->deductioncode!='' and $this->deductioncode!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.deductioncode = ".$this->deductioncode;
			       }
			       else
			       {
				         $cond=$cond." and t.deductioncode = ".$this->deductioncode;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select transactionnumber,
                        (h.fromdate||'--'||h.todate||'--'||f.farmernameuni ||'--'||
                         deductionnameuni||'--'||pendingamount)narr
                         from billdeductionpending t,
                         farmer f,deduction d,billperiodheader h
                         where t.farmercode=f.farmercode
                         and t.deductioncode=d.deductioncode
                         and t.billperiodtransnumber=h.billperiodtransnumber
                         and ".$cond." order by transactionnumber";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select transactionnumber,
                          (h.fromdate||'--'||h.todate||'--'||f.farmernameuni ||'--'||
                         deductionnameuni||'--'||pendingamount)narr
                         from billdeductionpending t,
                         farmer f,deduction d,billperiodheader h
                         where t.farmercode=f.farmercode
                         and t.deductioncode=d.deductioncode
                         and t.billperiodtransnumber=h.billperiodtransnumber
                          order by transactionnumber";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $this->transactiondate = DateTime::createFromFormat('d/m/Y',$this->transactiondate)->format('d-M-Y');
        $query = "
        update billdeductionpending set 
        transactiondate =".$this->invl($this->transactiondate,false)."
        ,billperiodtransnumber = ".$this->invl($this->billperiodtransnumber,true,true)."
        ,farmercode = ".$this->invl($this->farmercode,true,true)."
        ,deductioncode = ".$this->invl($this->deductioncode,true,true)."
        ,pendingamount = ".$this->invl($this->pendingamount,true,true)."
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $this->transactiondate = DateTime::createFromFormat('d/m/Y',$this->transactiondate)->format('d-M-Y');
        $query = "
        delete from billdeductionpending
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        transactionnumber
        ,transactiondate
        ,billperiodtransnumber
        ,farmercode
        ,deductioncode
        ,pendingamount
        from billdeductionpending where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->transactionnumber = $row['TRANSACTIONNUMBER'];
                $this->transactiondate = DateTime::createFromFormat('d-M-y',$row['TRANSACTIONDATE'])->format('d/m/Y');
                $this->billperiodtransnumber = $row['BILLPERIODTRANSNUMBER'];
                $this->farmercode = $row['FARMERCODE'];
                $this->deductioncode = $row['DEDUCTIONCODE'];
                $this->pendingamount = $row['PENDINGAMOUNT'];
                $this->billperiodtransnumbertextcombo = $this->textcomboname($connection=$this->connection,$table='BILLPERIODHEADER',$datafield='BILLPERIODTRANSNUMBER',$datafieldvalue=$row['BILLPERIODTRANSNUMBER'],$engfield='BILLPERIODNUMBER',$unicodefield='BILLPERIODNUMBER');
                $this->deductioncodetextcombo = $this->textcomboname($connection=$this->connection,$table='DEDUCTION',$datafield='DEDUCTIONCODE',$datafieldvalue=$row['DEDUCTIONCODE'],$engfield='DEDUCTIONNAMEENG',$unicodefield='DEDUCTIONNAMEUNI');
                $this->farmercodetextcombo = $this->textcomboname($connection=$this->connection,$table='FARMER',$datafield='FARMERCODE',$datafieldvalue=$row['FARMERCODE'],$engfield='FARMERNAMEENG',$unicodefield='FARMERNAMEUNI');
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

