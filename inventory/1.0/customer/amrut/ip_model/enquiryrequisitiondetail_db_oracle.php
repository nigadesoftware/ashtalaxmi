<?php
//This is generated by Nigade Software Technologies (opc) Private Limited 12/12/2020 10:11:00 am for PHP
require_once("../swappbase/formbase.php");
class enquiryrequisitiondetail extends swappform
{
    Public $transactionnumber;
    Public $serialnumber;
    Public $requisitiontransaction;
    public $requisitiontransactiontextcombo;
    public function __construct(&$connection)
    {
     		 parent::__construct($connection);
         $this->transactionnumber='';
         $this->transactionnumbertextcombo='';
         $this->requisitiontransaction='';
         $this->requisitiontransactiontextcombo='';
	   }
    public function entryvalidation()
    {
    		$this->start_validation();
        $this->checkrequired($this->requisitiontransaction,'Requisition Transaction','मागणी');
        $this->end_validation();
        return $this->invalidid;
	  }
    private function datavalidation()
    {
        $this->start_validation();
    		$this->invalidid=0;
    		$this->invalidmessagetext='Validated';
    		$this->end_validation();
    		return $this->invalidid;
    }
    public function insert()
    {
        $this->dataoperationmode = operationmode::Insert;
	    if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
	    if ($this->serialnumber == '')
        {
            $query = "select nvl(max(serialnumber),0)+1 as serialnumber 
            from enquiryrequisitiondetail where transactionnumber=". $this->transactionnumber;
            $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
            $row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS);
            $this->serialnumber = $row["SERIALNUMBER"];
        }
        $query = "
            insert into enquiryrequisitiondetail(
            transactionnumber
            ,serialnumber
            ,sectioncode
            ,requisitiontransaction
	        )
	        values (
            ".$this->invl($this->transactionnumber,true)."
            ,".$this->invl($this->serialnumber,true)."
            ,".$this->invl($this->sectioncode,true)."
            ,".$this->invl($this->requisitiontransaction,true,true)."
            )";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            $query1 = "delete from enquiryitemdetail e
            where e.transactionnumber=".$this->transactionnumber;
            $result = oci_parse($this->connection, $query1);
            if (oci_execute($result,OCI_NO_AUTO_COMMIT))
            {
                $query2 ="insert into enquiryitemdetail
                (transactionnumber, serialnumber, itemcode, enquiryquantity, itemspecification, employeecode)
                select t.transactionnumber,row_number() over (order by  d.serialnumber) srno
                ,d.itemcode,d.requisitionquantity,d.itemspecification,h.employeecode
                from ENQUIRYREQUISITIONDETAIL t,requisitionheader h,requisitiondetail d
                where t.requisitiontransaction=h.transactionnumber
                and h.transactionnumber=d.transactionnumber 
                and t.transactionnumber=".$this->transactionnumber;
                $result = oci_parse($this->connection, $query2);
                if (oci_execute($result,OCI_NO_AUTO_COMMIT))
                {
                    return 1;
                    exit;
                }
                else
                {
                    return 0;
                    exit;
                }
            }
            else
            {
                return 0;
                exit;
            }
            
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function display()
	  {
		    $this->dataoperationmode = operationmode::Select;
		    $cond='';
		    if ($this->requisitiontransaction!='' and $this->requisitiontransaction!=0)
		    {
			      if ($cond=='')
			      {
				        $cond="t.requisitiontransaction = ".$this->requisitiontransaction;
			       }
			       else
			       {
				         $cond=$cond." and t.requisitiontransaction = ".$this->requisitiontransaction;
			       }
		    }
        if ($cond!='')
		    {
		        $query = "select t.* from enquiryrequisitiondetail t where ".$cond." order by transactionnumber";
			      $result = oci_parse($this->connection, $query);
            $r = oci_execute($result);
			      return $result;
		     }
		     else
		     {
		         $query = "select t.* from enquiryrequisitiondetail t order by transactionnumber";
			       $result = oci_parse($this->connection, $query);
             $r = oci_execute($result);
			       return $result;
		     }
	  }
    public function update()
    {
        $this->dataoperationmode = operationmode::Update;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        update enquiryrequisitiondetail set 
        requisitiontransaction = ".$this->invl($this->requisitiontransaction,true,true)."
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
            return 1;
            exit;
        }
            else
        {
            return 0;
            exit;
        }
    }
    public function delete()
    {
        $this->dataoperationmode = operationmode::Delete;
        if ($this->entryvalidation() <> 0)
        {
            return 0;
            exit;
        }
        elseif ($this->datavalidation() <> 0)
        {
            return 0;
            exit;
        }
        $query = "
        delete from enquiryrequisitiondetail
        where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        ";
        $result = oci_parse($this->connection, $query);
        if (oci_execute($result,OCI_NO_AUTO_COMMIT))
        {
             return 1;
             exit;
        }
        else
        {
            return 0;
            exit;
        }
    }
    public function fetch()
    {
        $this->dataoperationmode = operationmode::Select;
        $query = "select 
        transactionnumber
        ,serialnumber
        ,requisitiontransaction
        ,sectioncode
        from enquiryrequisitiondetail where 
        transactionnumber = ".$this->invl($this->transactionnumber,true)."
        and serialnumber=".$this->invl($this->serialnumber,true);
        $result = oci_parse($this->connection, $query);
        $r = oci_execute($result);
        if ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
        {
                $this->transactionnumber = $row['TRANSACTIONNUMBER'];
                $this->serialnumber = $row['SERIALNUMBER'];
                $this->requisitiontransaction = $row['REQUISITIONTRANSACTION'];
                $this->requisitiontransactiontextcombo = $this->textcomboname($connection=$this->connection,$table='requisitionheader',$datafield='transactionnumber',$datafieldvalue=$row['REQUISITIONTRANSACTION'],$engfield='REQUISITIONNUMBER',$unicodefield='REQUISITIONNUMBER');
                $this->sectioncodetextcombo = $this->textcomboname($connection=$this->connection,$table='section',$datafield='sectioncode',$datafieldvalue=$row['SECTIONCODE'],$engfield='sectionnameeng',$unicodefield='sectionnameuni');
                $this->found=true;
                return true;
        }
        else
        {
        return false;
        }
    }
}
?>

