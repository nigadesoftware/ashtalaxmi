<?php
    //This is generated by Swapp Software Application on 12/12/2020 10:07:53 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select SUPPLIERCODE,
       SUPPLIERNAMEUNI,
       SUPPLIERNAMEENG
       from SUPPLIER where rownum <= 100";
   }
   else
   {
       $query = "select SUPPLIERCODE,
       SUPPLIERNAMEUNI,
       SUPPLIERNAMEENG
       from SUPPLIER where (upper(SUPPLIERNAMEENG) like upper('%".$searchTerm."%')
       or SUPPLIERNAMEUNI like '%".$searchTerm."%'
       or to_char(SUPPLIERCODE) like '".$searchTerm."') and rownum <= 100";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['SUPPLIERCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['SUPPLIERNAMEENG'];
       }
       else
       {
           $name = $row['SUPPLIERNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>