<?php
    //This is generated by Swapp Software Application on 12/12/2020 10:10:33 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    $seasoncode = $_SESSION['yearperiodcode'];
    
        
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }

   if ($searchTerm == '**')
   {
       $query = "select h.transactionnumber,h.indentprefixnumber
       from indentheader h
       where h.financialyear=".$_SESSION['yearperiodcode']." 
       and h.transactionnumber not in (select i.indenttransactionnumber
       from issueheader i)";
   }
   else 
   {
       $query = "select h.transactionnumber,h.indentprefixnumber
       from indentheader h
       where h.financialyear=".$_SESSION['yearperiodcode']." 
       and h.indentprefixnumber like '%".$searchTerm."%'
       and h.transactionnumber not in (select i.indenttransactionnumber
       from issueheader i)";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $transactionnumber = $row['TRANSACTIONNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['INDENTPREFIXNUMBER'];
       }
       else
       {
           $name = $row['INDENTPREFIXNUMBER'];
       }
       $data[] = array( 
           'id' => $transactionnumber
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

