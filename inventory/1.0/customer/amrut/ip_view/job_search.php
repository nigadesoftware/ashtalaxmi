<?php
    //This is generated by Swapp Software Application on 12/12/2020 10:32:49 am for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "select JOBCODE,
       JOBNAMEUNI,
       JOBNAMEENG
       from JOB where rownum <= 10";
   }
   else
   {
       $query = "select JOBCODE,
       JOBNAMEUNI,
       JOBNAMEENG
       from JOB where (upper(JOBNAMEENG) like upper('%".$searchTerm."%')
       or JOBNAMEUNI like '%".$searchTerm."%'
       or to_char(JOBCODE) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['JOBCODE'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['JOBNAMEENG'];
       }
       else
       {
           $name = $row['JOBNAMEUNI'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

