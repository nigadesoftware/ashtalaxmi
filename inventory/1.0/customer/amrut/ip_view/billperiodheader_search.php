<?php
    //This is generated by Swapp Software Application on 27/11/2022 03:13:52 pm for PHP
    require_once("../info/phpsqlajax_dbinfo.php");
    require_once("../info/phpgetloginview.php");
    require_once("../info/routine.php");
    $connection =swapp_connection();
    $searchTerm = $_GET['term'];
    if (preg_match('/^[0-9]*$/', $searchTerm))
    {
    }
    elseif (strlen($searchTerm)<3 and $searchTerm != '**')
    {
        exit;
    }
   if ($searchTerm == '**')
   {
       $query = "
       select BILLPERIODTRANSNUMBER,
       (to_char(h.fromdate,'dd-MM-yyyy')||' To '||to_char(h.todate,'dd-mm-yyyy'))period,
       (to_char(h.fromdate,'dd-MM-yyyy')||' To '||to_char(h.todate,'dd-mm-yyyy'))period
       from BILLPERIODHEADER h
       where rownum <= 10";
   }
   else
   {
       $query = "select BILLPERIODTRANSNUMBER,
       (to_char(h.fromdate,'dd-MM-yyyy')||' To '||to_char(h.todate,'dd-mm-yyyy'))period,
       (to_char(h.fromdate,'dd-MM-yyyy')||' To '||to_char(h.todate,'dd-mm-yyyy'))period
       from BILLPERIODHEADER h
        where (upper(BILLPERIODNUMBER) like upper('%".$searchTerm."%')
       or BILLPERIODNUMBER like '%".$searchTerm."%'
       or to_char(BILLPERIODTRANSNUMBER) like '".$searchTerm."') and rownum <= 10";
   }
   $result = oci_parse($connection, $query); $r = oci_execute($result);
   $r = oci_execute($result);
   while ($row = oci_fetch_array($result,OCI_ASSOC+OCI_RETURN_NULLS))
   {
       $id = $row['BILLPERIODTRANSNUMBER'];
       if ($_SESSION['lng'] == "English")
       {
           $name = $row['PERIOD'];
       }
       else
       {
           $name = $row['PERIOD'];
       }
       $data[] = array( 
           'id' => $id
           ,'label' => $name
           ,'value' => $name
           );
   }
   //return json data
   echo json_encode($data);
?>

